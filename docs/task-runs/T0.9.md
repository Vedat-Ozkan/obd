# T0.9 task run

## Ownership

- Last tool: Claude
- Last role: orchestrator (preflight)
- Current stage: Stage A CLOSED 2026-09-24; Stage B waits for the owner's Equinox run
- Completed stages: preflight
- Other active tool or role: Codex T2.2 (uncommitted: `packages/obd-core/scripts/replay.ts`, `test/obdb-equinox.test.ts`, `src/vehicles/obdb/equinox.ts`, T2.2 docs). T0.6a is approved but still uncommitted in the `/home/kema/projects/obd-t06` worktree.

## Contract

- Task: PLAN T0.9. App codes report flow: connect → scan → report → share as markdown/PDF through the share sheet. It must handle `UNABLE TO CONNECT` and disconnects without hanging. It uses the T0.7 codes report (`packages/obd-core/src/report/`). The same run records a session that fills the counters leg the Equinox recordings lack (`0120 0140 0101 0130 0131 07 0A`, per the T0.7 B2 review), so the car session gives real counter values.
- Verify (PLAN): manual run on the Equinox EV; one session recording added and replayed; a replay test asserts the report fields.
- Spec paths: `docs/specs/T0.9-codes-report-flow.md` (Stage A software-only first; Stage B after the owner's Equinox run)
- Approved decisions: owner "anything else we can work on before a car session?" (2026-09-24), under the standing delegation.
- Repair count: 0 of 2. Escalation: 0.

## Baseline

- `main` at `04d0f0a`. The installed dev client is EAS build `0f877dca` (JS-only changes keep it valid).

## Architecture

- 2026-09-24, Claude architect: wrote `docs/specs/T0.9-codes-report-flow.md`. Stage A (8 files; JS-only, one `obd-core` `exports` line, no dependency/lockfile/native change): move `codesReportFromRecording` into `src/report/replay.ts` (`obd-core/report`), `runCapture` options (`commands`, `stopAfter`), `apps/mobile/src/codesScan.ts` (19 fixed commands, stop rules, markdown heading), Run codes report + Share report in the Equinox-only console. E2E: the flow over a fake answering from the tracked spike and phone-console recordings equals heading + their block in `packages/obd-core/test/codes-reports.md`; isolated 1–5 (UNABLE TO CONNECT, disconnect, timeout without `>`, ATZ STOPPED, ATZ timeout + late `>`). Stage B (5 files) after the hardware run. Open questions 1–7 await decisions (PDF deferred recommended). No code written; nothing run.

## Next action

- Orchestrator: decide open questions 1–7 (Q1 PDF is the owner's call on the PLAN row), then implementer Stage A.

- 2026-09-24: open questions decided (spec Decisions; Q1 PDF by the owner). Stage A implementer spawned.

## Stage A implementation

2026-09-24, Claude implementer. Decisions applied: Q1 markdown only (no PDF, no dependency); `"./report"` added next to `"./transport/relay"` (T0.6a already on main).

- Tests first: `apps/mobile/test/codes-scan.test.ts` (E2E over spike + phone-console redacted recordings; isolated 1–5) failed on the pre-change source (`../src/codesScan.js` missing).
- Move: `codesReportFromRecording` now in `packages/obd-core/src/report/replay.ts` (verbatim body; re-exports `renderCodesReport`, `CodesReport`); `scripts/codes-report.ts` imports and re-exports it. `node --import tsx packages/obd-core/scripts/codes-report.ts <7 T0.7 PATHS> | diff - packages/obd-core/test/codes-reports.md`: empty, exit 0. `test/codes-report.test.ts` unchanged, 12/12 pass.
- `runCapture(..., options?: CaptureOptions)`: `commands`, `stopAfter` (after a resolved reply and after a late `>` on the rejected path). `capture.test.ts` unchanged, C1–C7 pass.
- `apps/mobile/src/codesScan.ts`: 19 commands, `codesScanStop` rules 1–2, `codesReportMarkdown`. `App.tsx`: Run codes report, report view, Share report, `localFilename(slug, extension)`, `exportFile(...)`.
- `apps/mobile` vitest: 67/67 (codes-scan 7/7).
- Mutation checks on a temp copy outside the repo (deleted afterwards): drop rule 2 → isolated 1 fails; drop rule 1 → isolated 4 (and 5) fail; runner ignores `options.commands` → both E2E cases fail (plus isolated 2, 3); no `stopAfter` on the rejected path → isolated 5 fails.
- Purity grep over `packages/obd-core/src`: empty. Only `transport.write` in `apps/mobile/src` is `console.ts:77`.
- `expo export --platform android` to a temp dir: exit 0 (727 modules, Hermes bundle); output deleted.
- `git diff -- apps/mobile/package.json apps/mobile/app.json pnpm-lock.yaml`: empty. `git diff --check`: clean.
- `pnpm check`: fails in the shared working tree ONLY on untracked T2.5 work in progress (`packages/obd-battery/test/twelve-volt-report.test.ts` TS2307, the module it tests does not exist yet; not T0.9's). In a temp copy without `packages/obd-battery/`, `pnpm check:ts` exits 0 (obd-core 185, mobile 67, relay 58); `check:bridge` passes in the repo.
- Hardware (Stage B prerequisites, hardware steps 1–10): NOT RUN, needs the owner's Equinox session.

### Stage A round 1 (Claude reviewer, 2026-09-24): APPROVE

- 12/12 Stage A items. `pnpm check` PASS on a temp copy without Codex's in-progress `packages/obd-battery/` (obd-core 186, mobile 67, relay 58). The artifact regenerates byte-identically; the E2E compares against the committed artifact; all four mutants are caught; `expo export` OK. Commands are sourced and pass both the allowlist and the guard; no 04/09/22 is sent and no VIN is printed; nothing can hang.
- Minors, both closed by the orchestrator:
  - 1. The "lockfile diff empty" line is true for T0.9's files. The current `pnpm-lock.yaml` hunk (`packages/obd-battery`) belongs to Codex T2.5 and is not in the T0.9 commit.
  - 2. Deviations, all accepted: the `localDate()` helper; the `exportJsonl`/`shareReport` wrappers over `exportFile`; the "Share sheet opened / Share error" status text; the codes run reusing the capture summary; Share report disabled during a scan; the `@ts-expect-error` `node:fs` pattern in the test; `pnpm check` run on a temp copy.
- Stage A CLOSED. Stage B (the owner's Equinox run, the redacted recording, the 8th T0.7 path) is NOT RUN.

## Equinox phone run (owner, 2026-09-24; reconciled by the orchestrator)

- The in-app report built on the phone (Hermes) from a 19-command scan: "Scan complete: 19 of 19 commands sent". This resolves the spec's biggest risk: `Elm327Session` + `ReplayTransport` run under Hermes. The shared report is `fixtures/recordings/chevrolet-equinox-ev-2024/2026-09-24-codes-report.md` (SHA-256 `c9e6ddc2…ae77`, local, untracked). It has no VIN.
- Content: 5 modules (17, 28, 40, 45, CB). No stored or pending codes. Components monitors complete. Module 17: 157 warm-ups and 8176 km since codes cleared; module 28: 158 and 8174 km. PID 4E is unsupported. Permanent codes: 17 and CB answered negative 11; 28/40/45 none. Freeze frame on 17: none stored. **Verdict: not indicated.** This is the first real counter values: every read counter is at or above policy.
- The exported recording `2026-09-24-phone-console-2.jsonl` (SHA-256 `0c3d141a…9598`) is a one-button **capture** run (10 commands, `capture complete: 10 of 10 sent`), **not** the codes scan. It replays green: 10 commands, 0 errors. `redact_vin.py`: no VIN, `.redacted.jsonl` SHA-256 `700a0734…9c67ff`. Windows `:Zone.Identifier` side files deleted.
- Still missing for hardware steps 5–10 and Stage B: the codes-scan recording (meta `capture start: ATZ … 0120 0140 0101 0121 0130 0131 014D 014E 03 07 0A 020200`), which is needed to check that the phone report equals `pnpm -s codes-report` on its redacted copy.

- 2026-09-24, orchestrator: T0.9b (`docs/specs/T0.9b-one-and-done-captures.md`) supersedes Decision 5 and hardware steps 3 and 5. Stage B's codes-scan recording comes from T0.9b hardware step 8.
