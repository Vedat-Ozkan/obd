# T0.4 task run

## Ownership

- Last tool: Claude
- Last role: reviewer (round 3: APPROVE, 2026-09-23)
- Current stage: CLOSED 2026-09-23 (hardware items NOT RUN by design, ADR-015); ready to tick
- Completed stages: preflight, architecture (Claude architect 2026-09-23), open questions answered, scope confirmation (owner: "redact vin then implement"), prerequisite X-2026-09-23-vin-redaction closed (APPROVE), implementation (Claude implementer 2026-09-23)
- Other active tool or role: none

## Contract

- Task: T0.4 — `obd-core` `Elm327Session`: init sequence, protocol select with fallback, single-flight command queue, per-command timeout, retry policy, ISO-TP multi-frame reassembly with headers on, response routing by ECU header. Verify (PLAN): replay tests cover Equinox init, a multi-frame VIN, a `SEARCHING...` first request, and a timeout; synthetic fixtures cover branches absent from the EV recording; `pnpm replay` prints decoded frames.
- Spec paths: `docs/specs/T0.4-elm327-session.md`
- Approved decisions: task started by the user 2026-09-23 while the car is unavailable (T0.8 phone session pending); ADR-015 (Equinox is the required vehicle; ICE recordings not required); spec open questions 1–3 answered as recommended (fallback now, session write guard, `tsx`); VIN in tracked recordings: owner chose redact + ADR as a separate task before T0.4 implementation
- Repair count: 2 of 2 (last allowed)
- Escalation count: 0
- Escalation reason: none

## Baseline

- Git state at start: `main` at `c88dffa`; `git status --short` empty.
- Inputs: T0.3 `Transport`, `ReplayTransport`, `ElmLineReader` in `packages/obd-core/src`; tracked Equinox recordings `fixtures/recordings/chevrolet-equinox-ev-2024/2026-09-22-spike.jsonl` and `-spike-2.jsonl`; local gitignored discovery recordings (2026-09-23) that tests on this machine also replay; T0.3 review note: the ELM clone's reset garbage byte `ü` survives as a content line and must be discarded in the session layer.
- Constraint: no car or Bluetooth in this shell; everything here is replay- and fixture-based.

## Touched files

- `docs/task-runs/T0.4.md` (this record)
- `docs/specs/T0.4-elm327-session.md` (architect; Decisions by orchestrator)

## Verification evidence

- none yet

### Implementation (Claude implementer, 2026-09-23)

- Stages A and B built (10 files per the spec). `pnpm -F obd-core test` PASS (5 files, 80 tests) with five deliberate mutations each caught; `pnpm check` PASS; purity grep PASS; `git diff --check` PASS; only `tsx` (+ esbuild) added.
- `pnpm replay fixtures/recordings/chevrolet-equinox-ev-2024/2026-09-22-spike.redacted.jsonl`: `summary: 32 commands; data 15, ok 15, nodata 1, error 1 (buffer-full=1), timeout 0; frames 41; dropped malformed=1, incomplete=4, no-first-frame=1` (spike-2 identical); synthetic fixture and local discovery replays exit 0.
- Hardware items NOT RUN by design (ADR-015). Deviations flagged: spike lines read from redacted files in tests; `ü` in the synthetic fixture (round-3 reviewer: stored as raw UTF-8, not the JSON escape the note claimed; effect identical — dropped as non-printable, kept in `raw`); replay prints the frame's actual first bytes before `<N bytes redacted>`; empty-summary wording; one eslint-disable for write-rejection propagation; FF with < 2 data bytes → `malformed`; interface block includes referenced types; `ü` test via a fresh `ATZ`; CLI exits 1 on any Error.
- **Privacy finding:** `pnpm replay` printed the full VIN from `22 4193` on the local discovery recordings (output only to deleted /tmp files; not displayed). Orchestrator amendment appended to the spec: redact `22 4193` too, plus a synthetic test. Implementer continues before review.

- Amendment (22 4193 replay redaction): `replay.ts` `VIN_REPLIES` (`0902` keeps 2 bytes, `224193` keeps 3); new synthetic test (fails without the `224193` entry). `pnpm -F obd-core test` PASS (81); `pnpm check` PASS; `git diff --check` PASS. Local discovery replays with output to a deleted /tmp dir: no VIN form found in either (runtime check, VIN never displayed); 1 and 9 redacted `62 41 93` frames; summaries unchanged (discovery: `16882 commands; … timeout 1 …`; targeted: `10434 commands; … timeout 0 …`).

## Review findings

### Round 1 (Claude reviewer, 2026-09-23): REQUEST_CHANGES

1. [blocking, fixed in repair 1] `session.ts:106` — write guard checks only the first two characters after stripping spaces and then writes `cmd + "\r"`; an embedded CR (`send("0100\r04")`, `send("\r04")`) puts `04` on the wire; `ATSP${profile.protocol}` unchecked at runtime. Spec amended: reject any non-printable (outside 0x20–0x7E) before normalizing; validate `profile.protocol` ∈ {0,6,7}; tests.
2. [minor, fixed in repair 1] no test for the retry after a `sequence` drop.
3. [minor, fixed in repair 1] no test for a rejected `transport.write` (propagate unchanged, mark stale).
4. [minor, fixed in repair 1] `replay.ts:39` — leftover VIN consecutive frames from a cut-short VIN reply would print in the next response's `dropped no-first-frame` lines (0 occurrences in current recordings); spec amended to redact them.
5. [minor, deferred] only `04`/`2E`/`2F`/`31` blocked; other UDS reset/clear services need a sourced service table — note for T0.7 and the relay.
- Otherwise: 9/9 verification items satisfied; nine deviations accepted; sources, purity, lockfile, single-flight, close, fallback, reassembly, BUFFER FULL verified; local discovery replays (output to deleted /tmp, summaries only) show no VIN.

### Repair round 1 (Claude implementer, 2026-09-23)

- `session.ts`: non-printable (outside 0x20–0x7E) rejection first in `send()`; `init()` validates `profile.protocol` ∈ {0,6,7}. `replay.ts`: redacts `dropped` lines in the response after `0902`/`22 4193`. Tests: 9 extended (`0100\r04`, `\r04`, `01\n00`, protocol `0\r04`, zero writes), 10 (sequence retry), 11 (write rejection propagates, stale until RESYNC_MS), leftover-VIN replay test for both commands.
- Mutations each caught (guard, protocol check, sequence retry, stale flag, dropped-line redaction). `pnpm -F obd-core test` PASS (85); `pnpm check` PASS; `git diff --check` PASS; purity grep PASS; spike replays unchanged; local discovery replays: no VIN form in output (deleted /tmp, summaries only).
- Deviations: blocked `init()` reports command `ATSP<protocol>`; tests 10–11 numbered after 9. Question: unify `toResponse()`'s inline printable filter with `NON_PRINTABLE`.

### Round 2 (Claude reviewer, 2026-09-23): REQUEST_CHANGES

1. [blocking, CLOSED round 3] `session.ts:106-109,237,240` — `init()` validates `profile.protocol` at call time but the queued `runInit` re-reads it later; a mutated profile or a getter writes `ATSP0\r04\r` (reproduced with a temp script outside the repo, deleted). Fix: read once, validate, pass only the string; add a test mutating the profile after `init()`; also check non-printables in `exchange()` right before the single `transport.write`.
2. [major, routed to T0.7/relay, no change here] with `ATCAF0` the caller supplies the PCI byte, so `send("0104")` could carry service 04 in one frame; the relay allowlist or T0.7 must block `ATCAF0` or check the service after the PCI when formatting is off; CAF0 semantics need a source first. Recorded with round-1 finding 5.
3. [minor, CLOSED round 3] unify `toResponse`'s inline printable regex with `NON_PRINTABLE`.
- Round-1 findings 1 (send path), 2, 3, 4: fixed, and the new tests fail on the pre-repair code. Adversarial send() inputs (Unicode lookalikes, spaced, lowercase, long) blocked or harmless. 9/10 items; unmet: finding 1.

### Repair round 2 (Claude implementer, 2026-09-23)

- `init()` reads `protocol` once, validates it, passes only the string to `runInit`; `exchange()` rejects non-printable commands before `arm()` and the single `transport.write`; `toResponse` uses `NON_PRINTABLE`. Tests 12 (mutation after `init()`) and 13 (getter) fail on the round-2 code and pass now; with only the exchange guard they fail as `blocked` (guard works alone).
- `pnpm -F obd-core test` PASS (87); `pnpm check` PASS; `git diff --check` PASS; purity grep PASS; spike replays byte-identical to the pre-repair baseline; recordings untouched.

### Round 3 (Claude reviewer, 2026-09-23): APPROVE

- Round-2 findings 1 and 3 fixed; tests 12/13 fail on the round-2 code (verified on temp copies outside the repo). Write-path audit: single `transport.write`; retries resend the guarded command; resync, retry delay and `close()` write nothing; 24 adversarial inputs blocked with zero writes. 8/8 items PASS; `pnpm check` PASS (obd-core 87, mobile 50).
- Local discovery summaries (full, no VIN; output to a deleted temp dir): discovery `summary: 16882 commands; data 16622, ok 216, nodata 40, error 3 (buffer-full=3), timeout 1; frames 16619; dropped incomplete=3, no-first-frame=5`; targeted `summary: 10434 commands; data 9860, ok 534, nodata 39, error 1 (buffer-full=1), timeout 0; frames 9766; dropped incomplete=1, no-first-frame=1`.
- Open items routed to a follow-up before or with T0.7 (write-safety): (a) [major] `send("")`/whitespace-only writes a bare CR, which on ELM327 reportedly repeats the last command (reviewer's datasheet memory, needs a checked-in source) — reject empty normalized commands as `blocked`, add a test and a sourced ELM327.md row; (b) UDS `14`, `11` and other reset/clear services; (c) `ATCAF0` with caller-supplied PCI. Minor: the two non-printable checks mask each other in tests (inner one is private; accepted). Fixture line-4 encoding note corrected above.

## Blocker

- None.

## Next action

- Owner ticks T0.4 in PLAN and commits.
- Follow-up write-safety task (empty-send repeat, UDS reset/clear services, ATCAF0) before or with T0.7.
