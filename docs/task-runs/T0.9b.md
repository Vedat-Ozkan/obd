# T0.9b task run

## Ownership

- Last tool: Claude
- Last role: architect
- Current stage: implementation
- Completed stages: preflight
- Other active tool or role: Codex T2.5 has an uncommitted one-line change in `apps/mobile/App.tsx` (the T2.5b unplug notice). It must be kept.

## Contract

- Task: owner requirement 2026-09-24: "when doing these sorts of data captures i want it one and done. no start/export recording buttons. the run capture button should run what it needs to, and then export cleanly. similarly with run report."
  - **Run capture** and **Run codes report** each run their commands and then save every output by themselves: the raw recording, and for the codes run also the report. There is no separate export step.
  - The Start/Stop recording and Export recording buttons are removed.
  - Cause: on 2026-09-24 the codes-scan recording was lost. The owner shared the auto-opened report, and the next Run capture discarded the unexported recording (see `docs/task-runs/T0.9.md`).
- Spec paths: `docs/specs/T0.9b-one-and-done-captures.md`
- Approved decisions: the owner's requirement above; standing delegation for the details.
- Repair count: 0 of 2. Escalation: 0.

## Log

- 2026-09-24, Claude architect: wrote `docs/specs/T0.9b-one-and-done-captures.md`. The design is JS-only with the installed expo-file-system 57.0.7 / expo-sharing 57.0.21, cited from node_modules. Every run first keeps a private copy under `Paths.document/captures/`. It then writes to an Android SAF folder that the owner picks once (`Directory.pickDirectoryAsync`, a persistable grant), with the share sheet one file at a time as the fallback. `shareAsync` takes a single URL, so there is no multi-file share. A codes run saves the recording first, then the report. The new pure module `apps/mobile/src/runFiles.ts` (`finishRun`) is testable in Node. Start/Stop, Export recording and Share report are removed, and Send becomes a debug session that is not saved (a new session each time, still gated by the ATZ/ATI unknown-state rule). There are 6 files, E2E E1–E5 over the existing fake transports, isolated I1–I5 and an owner phone checklist. There are 5 open questions with recommendations. No code was written and nothing was run apart from `pnpm -F mobile test`/`typecheck` (baseline green: 71 tests).

## Next action

- Orchestrator: decide open questions 1–5, then the implementer.

- 2026-09-24, orchestrator: open questions 1–5 decided as recommended (spec Decisions). Implementer spawned.
- 2026-09-24, implementer: stopped by an API session limit mid-work (last output "Now the fake targets."). Partial work in the tree: `src/runFiles.ts`, `test/fakeTargets.ts`, `test/run-files.test.ts`, and E1–E5 additions in `test/capture.test.ts` and `test/codes-scan.test.ts`. `App.tsx` not yet touched. Nothing verified.
- 2026-09-25, orchestrator: Codex committed T2.5 (`61c1407`), so the App.tsx unplug-notice line is now committed. Codex T2.6a is in flight in the same tree (`apps/mobile/package.json`, `src/battery*.ts`, `test/battery-*.test.ts`, obd-core/obd-battery package files, `pnpm-lock.yaml`) and waits on T0.9b to release `App.tsx`. Implementer re-spawned to reconcile and finish.
- 2026-09-25, implementer: partial work reviewed and kept unchanged; App.tsx done (phoneTargets, capture → finishRun, "Send (not saved)" debug session; Start/Stop/Export recording and Share report removed). PASS: mobile test (90), mobile typecheck, eslint, pnpm check, git diff --check, expo export android, mutations (a)–(e) each fail the named tests. NOT RUN: owner phone checklist (hardware-only). Deviation: the spec's package.json/lockfile baseline is stale because of Codex T2.6a; not touched by T0.9b.
- Next action: independent reviewer.
- 2026-09-25, reviewer: **APPROVE**. Reran pnpm check (PASS), expo export (PASS), the T0.7 artifact diff (exit 0) and all five mutations (each fails the named tests; runFiles.ts restored byte-identical). Minor findings, accepted as follow-ups: (1) the test-first step was not recorded: the interrupted first run wrote runFiles.ts and the tests in one pass, so there is no evidence the tests failed on the pre-change source; the mutations partly make up for it. (2) `App.tsx` `share` rebuilds the path with a second `"captures"` literal instead of reusing `keep`'s directory. (3) `fakeTargets.ts` error fields are settable but never overridden.
- Stage: implementation and review closed. Owner phone checklist (spec steps 1–9) NOT RUN, hardware-only; T0.9 Stage B takes its codes-scan recording from step 8.
- 2026-09-25, orchestrator: committed by path (AGENTS.md now lets the orchestrator commit after APPROVE).
