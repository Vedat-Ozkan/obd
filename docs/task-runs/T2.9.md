# T2.9 task run

## Ownership

- Last tool: Claude
- Last role: architect (spec written 2026-09-25)
- Current stage: architecture done. Waiting for open questions 1–10 and for the owner to accept or amend ADR-019 (Proposed).
- Completed stages: preflight (architect, read-only), architecture
- Other active tools and roles:
  - **Codex, T2.6b/c/d (active).** It holds `apps/mobile/App.tsx`. It has uncommitted edits in `apps/mobile/package.json`, `apps/mobile/src/garage/catalog.ts`, `packages/obd-core/package.json`, `packages/obd-core/src/elm/session.ts`, `packages/obd-battery/package.json`, `pnpm-lock.yaml`, `docs/PLAN.md`, `docs/ARCHITECTURE.md` and `docs/DECISIONS.md`, plus new battery files.
  - **T2.4 (Claude).** Stage A files are untracked in `packages/obd-battery/`, and it will edit the root `package.json`. B1 will edit `apps/mobile/src/recording.ts`. B2 will edit `App.tsx`, `apps/mobile/package.json` and `pnpm-lock.yaml`.
  - No T2.9 stage edits any of those files until the owning role ends and the file is committed. The one exception is the ADR-019 append to the end of `docs/DECISIONS.md`, made by this architect with the rest of that file untouched.

## Contract

- **Task:** `docs/PLAN.md` T2.9, changed by the owner on 2026-09-25 to the automatic route. The proposed replacement PLAN row is in the spec; the orchestrator applies it after ADR-019 is accepted and Codex's `docs/PLAN.md` edits are committed.
- **Spec:** `docs/specs/T2.9-beta-data-upload.md`. **ADR:** ADR-019 (Proposed), at the end of `docs/DECISIONS.md`.
- **Owner-approved requirements carried in:**
  1. One consent screen at beta sign-up: what is collected, what is stripped, why, and how long it is kept. One "Share my data to improve the app" switch, default off, with a consent version recorded.
  2. After consent, uploads are automatic: each run's files upload, or queue offline and retry, with no extra taps. This plugs into `finishRun` in `apps/mobile/src/runFiles.ts` and into the battery-scan and charge-log ends.
  3. A settings switch to stop, and "Delete my data" to remove what was uploaded.
  4. Scrub before anything leaves the phone: the VIN (`redact_vin.py` rules ported to TS and tested against it), no location, month-only dates, and serial and part-number DIDs, each cited.
  5. Provenance on every file, zod-validated, with a stable pseudonymous tester key and vehicle key.
  6. Intake only through the recording tool, never by hand.
- **Stages and order:**

  | Stage | Where | Deliverable |
  |---|---|---|
  | A | desk, now | scrubber and provenance schema in `obd-core` |
  | B | desk | mobile outbox, consent state, and the `finishRun` upload target, against a fake backend |
  | C1 | desk | Worker and HTTPS client, in-process |
  | C2 | desk | `pnpm beta:intake` and the full-chain E2E |
  | D | phone and car | `App.tsx` wiring, checked against `wrangler dev` |
  | E | owner account, phone, car | deploy and one live run |

  Waits:
  - A waits for nothing.
  - B waits for Codex to commit `packages/obd-core/package.json`, and for T2.4 Stage A to be committed.
  - C1 waits for T2.4 Stage A's root `package.json` edit to be committed.
  - D waits for Codex to release `App.tsx`, `apps/mobile/package.json` and `pnpm-lock.yaml`. It also waits for T2.4 B2 to be committed or explicitly deferred; never concurrently.
  - E waits for owner steps 1–8.
- **App.tsx order:** Codex T2.6b/d → T2.4 B2 → T2.9 D. If D goes first, T2.4 B2 adds the `queue("charge-log", …)` call itself.
- **Backend recommendation:** Cloudflare R2 plus one Worker, with no accounts and a per-install secret. The architect's recollection of cost is about $0 to $1.30 a month at 50 testers in year one. Prices are **unverified**; owner step 2 checks them.
- **New dependencies:**
  - Stage D: declare `expo-modules-core` ~57.0.18. It is already installed and linked, so there is no rebuild.
  - Optional: `expo-network`, a native module that needs a new dev client, only if open question 5 chooses Wi-Fi-only charge logs.
  - `wrangler` runs via `pnpm dlx` and stays out of the lockfile.
- **Owner steps (outward-facing, not agent work):** spec §Owner steps 1–10. They cover prices, the Cloudflare account and R2, the bucket and lifecycle rules, deploy and the admin secret, logs off, the privacy contact and wording, `app.json` version bumps, recruiting testers, and a monthly intake run.
- **Owner rules carried in:** one-and-done (no extra taps after consent); read-only toward the vehicle (T2.9 sends no car command); never print raw gitignored recordings; only committed `*.redacted.jsonl` files are read; no secrets in the repo.
- **Approved decisions:** none yet.
- **Repair count:** 0 of 2 per stage. **Escalation count:** 0.

## Baseline

- Git: `main` at `d0af6cd` (T2.4 P1 committed the targeted redacted recording). The working tree holds Codex's T2.6 edits and T2.4's in-flight files, as listed under Ownership.
- Tests, run by the architect on 2026-09-25 at 12:11:
  - `pnpm -F obd-core test`: 198/198 PASS.
  - `pnpm -F mobile test`: 101/101 PASS. This includes Codex's in-flight tests.
- Inputs read:
  - Docs: AGENTS.md, CLAUDE.md, ARCHITECTURE.md, PLAN.md (T2.4, T2.8, T2.9, BM1, BM9), DECISIONS.md (ADR-007, 009, 014, 017, 018), ELM327.md, ML.md, EVAL.md, WORKFLOW.md, `docs/discovery-2026-09.md` §1–3.
  - Specs: T2.4 and T0.9b, and `X-2026-09-23-vin-redaction.md`.
  - Tools: `tools/spike/redact_vin.py` and its tests.
  - App: `apps/mobile/src/{runFiles,recording,capture,codesScan,batteryScan,batteryDiagnosisFlow,batteryReports,batteryReportsDocumentStore}.ts`, `garage/*`, and `App.tsx` (read only).
  - Package and config files: `obd-core` `recording/format.ts`, `obd/vin.ts` and `package.json`; root `package.json`, `tsconfig.json`, `.gitignore`, CI workflow, `app.json`.
  - Installed packages: `expo-file-system` 57.0.7 typings (`UploadTask`), and `expo-modules-core` `uuid` plus Android `CoreModule.kt`.
- Committed recordings: only tx line numbers were read, from `2026-09-23-discovery-targeted.redacted.jsonl`, and the heads of `2026-09-24-phone-console-2.redacted.jsonl`. **No gitignored raw recording was opened.**

## Touched files

- `docs/specs/T2.9-beta-data-upload.md` (architect, created)
- `docs/DECISIONS.md` (architect, ADR-019 appended at the end as Proposed; nothing else changed)
- `docs/task-runs/T2.9.md` (this record, created)

## Verification evidence

- None yet. Every item in the spec is NOT RUN.

## Review findings

- None yet.

## Blocker

- Open questions 1–10 and ADR-019 acceptance. Stage A needs only question 6 (scrub strictness) to be settled, because it fixes the rule list. It can start as specced if the owner accepts the recommendations.

## Next action

- Orchestrator: put open questions 1–10 and ADR-019 to the owner, and record the answers in the spec's Decisions. Then spawn the Stage A implementer. A's files are disjoint from Codex's and T2.4's.

## Decisions (orchestrator, 2026-09-25)

- The owner answered: R2 + Worker (ADR-019 accepted); 24-month retention; `checked` entries upload ("they tested it, they consented, upload"); any network, disclosed, no `expo-network`. Questions 4 and 6–10 were decided as recommended. See spec §Decisions and the ADR-019 Status line.
- Next action: Stage A (scrubber and provenance schema in `obd-core`) waits for Codex to commit its `packages/obd-core` changes (`package.json` exports, `src/vehicles/index.ts`, `src/elm/session.ts`). The owner's steps 2–7 (prices, account, bucket, deploy, logs off, contact address) can happen any time before Stage E.
- 2026-09-25, orchestrator: ADR-019 committed (`b547245`). Stage A implementer spawned. T2.4 B1 runs in parallel in disjoint files.
- 2026-09-25, Stage A implementer: stopped at a spec conflict. `vin-check-digit` plus the safety net refused the committed targeted recording: chance check-digit hits in digit runs in `22 2CA7`. Every other item passed, including the scrubber tests (recorded failing first), 3 mutations caught, and parity with `redact_vin.py` in CI and locally on spike, spike-2 and discovery.
- 2026-09-25, orchestrator: spec Decisions 11 (check-digit hits are masked only, not added to the safety net; the residual is accepted and counted), 12 (`ownership` in provenance), 13 (intake strips the provenance line before the idempotence check) and 14 (deviations accepted). Implementer resumed.
- 2026-09-25, Stage A implementer: done, not committed. PASS on:
  - E2E on 4 committed recordings. The targeted file masks the 9 `4193` messages, 7 Mode 09 infotypes, 1 odometer reply and 9 check-digit chance hits.
  - The artifact, regenerated twice byte-identical (`452e1393…`).
  - Parity with `redact_vin.py`, in CI and locally (counts only).
  - Tests 1–15, 13b (recorded failing first) and P1–P5.
  - 3 mutations, all caught.
  - obd-core tests 234/234, git diff --check.

  `pnpm check` lint fails only on T2.4 B1's in-flight files. Next action: reviewer.
- 2026-09-25, Stage A reviewer: **REQUEST_CHANGES**.
  - Blocking: failure mode 13 has no test for serials learned from 4193 or for the reassembled-payload check (mutations M3 and M6 survived).
  - Major: the meta allowlist drops T2.4's `event`/`reason`; tx/rx lines with extra keys pass through.
  - Minor: bad CFs in 09xx/22F1xx exchanges; no test that a changed line keeps `5.0`; the battery-scan note holds the power state; a duplicate serializer; misleading NOT RUN test titles.
  - Checks: pnpm check PASS, artifact byte-identical, parity PASS, 3 of 6 mutations caught.
- 2026-09-25, orchestrator: spec Decisions 15–19. Repair round 1 of 2 goes to a fresh implementer: 15, 16 and 17. 18 and 19 are recorded as follow-ups.
- 2026-09-25, repair implementer (round 1):
  - Decisions 15–17 are done.
  - Each new test failed before its fix; the extra test-13 cases add coverage and pass on the old code, so mutations M3 and M6 are what show they bite.
  - Mutations caught: M3, M6, M15a, M15b, M16, M4.
  - PASS: obd-core 236/236, eslint and tsc, artifact unchanged (`452e1393…`), local parity (counts only).
  - `pnpm check` passed once. Later reruns failed only on T2.4 B1's in-flight test file.
  - Reading of Decision 15 (confirmed by the orchestrator): `event` or `reason` alone refuses the file.
  - Next action: re-review.
- 2026-09-25, re-reviewer: **APPROVE**. It reproduced pnpm check (PASS), the artifact (`452e1393…`, cmp), parity in CI and locally (counts only), and 9 mutations caught. One minor follow-up: the lowercase-hex and rx-concat hex forms in the safety net have no test (mutations R2 and R5 survived). The provenance float form is not a defect.
- Stage A closed and committed by path. Follow-ups: Decisions 18 and 19, and the R2/R5 test gap. Next: Stage B (mobile consent, outbox, upload target), after T2.4 B1 commits `apps/mobile/src/recording.ts`.
