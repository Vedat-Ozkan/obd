# T2.4 task run

## Ownership

- Last tool: Claude
- Last role: architect (spec written 2026-09-25)
- Current stage: architecture done; waiting for open questions 1–7 (orchestrator and owner)
- Completed stages: preflight (architect read-only), architecture
- Other active tool or role: **Codex, T2.6b/T2.6c/T2.6d (active).** It holds `apps/mobile/App.tsx` and has uncommitted edits in `apps/mobile/package.json`, `apps/mobile/src/garage/catalog.ts`, `packages/obd-battery/package.json`, `packages/obd-core/package.json`, `packages/obd-core/src/elm/session.ts`, `pnpm-lock.yaml`, `docs/PLAN.md`, `docs/ARCHITECTURE.md` and `docs/DECISIONS.md`, plus new battery files (`git status`, 2026-09-25 ~11:55). T2.4 does not touch any of these until Codex's role ends and the file is committed.

## Contract

- Task: `docs/PLAN.md` T2.4 (ADR-016). Charging-session logger (Android foreground service, screen off, multi-hour) and the first capacity estimates from replay: integrated current ÷ ΔSOC, labeled BMS-SOC-referenced, and the BMS figure `27AF` ÷ SOC. Each has an error band and neither is called truth. Also cell imbalance from group spread, and rested windows at both ends.
- Verify line: one charge log on the Equinox with no gaps > 10 s in current and group voltages and rested windows at both ends; both estimates and error bands printed from replay.
- Spec: `docs/specs/T2.4-charge-logger.md`
- Stages and order:
  - **P1** (orchestrator, owner consent): commit a redacted copy of the T2.3b targeted recording.
  - **A** (desk): `obd-battery` replay, estimates and `pnpm charge-log`.
  - **B1** (desk): mobile runner in pure TypeScript and the streaming save.
  - **B2** (desk, then phone and a short driveway check): foreground-service dependency, config plugin, `App.tsx` button, new EAS dev client.
  - **C** (car): the one real charge log.

  A can start as soon as P1 is done; its files are disjoint from Codex's. B1 waits for T2.6 to commit `packages/obd-battery/package.json` and for T2.6c to commit `packages/obd-core/src/elm/session.ts`. B2 waits for T2.6b/d to release `App.tsx`, `apps/mobile/package.json` and `pnpm-lock.yaml`.
- Owner rules carried in: one-and-done captures (one tap; the run saves every file itself; no start, stop or export buttons); read-only toward the vehicle; never print raw gitignored recordings; committed `*.redacted.jsonl` only.
- New dependency (B2 only, pending open question 2): `react-native-background-actions@4.1.0` (MIT). **B2 invalidates the installed dev client (EAS `0f877dca`). The owner must build and install a new dev client before the B2 hardware check and Stage C.**
- Approved decisions: none yet. Open questions 1–7 are in the spec, with recommendations.
- Repair count: 0 of 2 (per stage). Escalation count: 0.

## Baseline

- Git: `main` at `1b4364b`. Codex's T2.6 work is uncommitted (see Ownership).
- `pnpm -F obd-battery test`: 10/10 PASS. `pnpm -F mobile test`: 95/95 PASS (2026-09-25 11:51, run by the architect; includes Codex's in-flight tests).
- Inputs read: PLAN T2.4, ML.md, EVAL.md, ARCHITECTURE.md, ELM327.md, DECISIONS (ADR-013 to ADR-018), task-runs/T2.3b.md, discovery-2026-09.md §5 and §7, `apps/mobile/App.tsx`, `src/capture.ts`, `src/runFiles.ts`, `src/recording.ts`, the OBDb Equinox signalset, `tools/spike/targeted_watch.json`. The installed React Native 0.86.3 `JavaTimerManager.kt` and `HeadlessJsTaskService.kt`, the `react-native-ble-plx` plugin, `expo-file-system` 57.0.7 typings, the `react-native-background-actions` 4.1.0 tarball (read in the scratchpad, not installed) and the Android FGS types page. No gitignored raw recording was opened.

## Touched files

- `docs/specs/T2.4-charge-logger.md` (architect, created)
- `docs/task-runs/T2.4.md` (this record, created)

## Verification evidence

- None yet. Every item in the spec is NOT RUN.

## Review findings

- None yet.

## Blocker

- Open question 1 (P1). Without the redacted targeted recording, `2414`, `2885`, `27AF` and the `2AE1`–`2AE7` layout have no committed source, so Stages A and B cannot be built (hard rule 1).

## Next action

- Orchestrator: put open questions 1–7 to the owner and record the answers in the spec's Decisions. If P1 is approved, run P1 (spec §Verification P1), then spawn the Stage A implementer.

## P1 and decisions (orchestrator, 2026-09-25)

- Owner answered open questions 1 (commit the redacted targeted recording: yes), 2 (`react-native-background-actions@4.1.0`, Kotlin module as fallback) and 4 (B2 driveway check: yes). 3, 5, 6 and 7 decided as recommended. Recorded in the spec's Decisions.
- P1 PASS: original SHA-256 `4df9d935…d6cc` before and after; `uv run tools/spike/redact_vin.py` wrote `fixtures/recordings/chevrolet-equinox-ev-2024/2026-09-23-discovery-targeted.redacted.jsonl` (84,155 lines; last meta names the source SHA-256, script version 1, 9 masked messages; redacted file SHA-256 `35765599…379c`). `.gitignore` has one exception line; `git status` un-ignores no other recording. `pnpm -F obd-core exec vitest run test/recordings.test.ts` 19/19 PASS, the new file replays strictly.
- Next action: Stage A implementer (no Codex file overlap). B1 waits for Codex's T2.6 commits of `packages/obd-battery/package.json` and `obd-core/src/elm/session.ts`; B2 waits for `App.tsx`, `apps/mobile/package.json` and the lockfile.
- 2026-09-25, Stage A implementer: stopped before writing tests, as the spec requires. The layout check found that the spec's "group min/max equal `2AF5`" claim holds in only 27 of 111 cycles. No repo change was made.
- 2026-09-25, orchestrator: the spec is fixed under standing delegation. Decision 8: agreement is an exact-equality statistic, not a gate, with no tolerance chosen after seeing the data. Decision 9: the session-boundary meta line shape. Stage A implementer re-spawned.
- 2026-09-25, Stage A implementer: done, not committed. The tests were written first and recorded failing. PASS: synthetic and real E2E (artifact `cmp`-equal), generator byte-identical over 3 runs, artifact regenerated twice identical, recordings.test.ts 20/20, obd-battery 14/14, pnpm check, git diff --check. Real P1 gate FAIL as expected (current in rotation, 98.9 s gap), agreement 27/111. 11 deviations are listed in the hand-back and go to the reviewer. The implementer raised two questions: the post-rest minimum in the gate, and the synthetic label schema.
- Next action: independent reviewer.
- 2026-09-25, Stage A reviewer: **REQUEST_CHANGES**, 1 blocking finding. With a post-charge rest of 0 s or 40 s the gate prints PASS and BM2 pass (reproduced with the synthetic fixture cut short). 6 minor findings: the CLI path guard, the empty builder lists, the not-80 branch not covered, the dead `currentAt` fallback, the label schema, and spec notes for the deviations. pnpm check PASS; the artifact and the generator were reproduced byte-identical; 3 mutations were caught.
- 2026-09-25, orchestrator: spec Decisions 10 (the post-rest minimum in the gate and BM2), 11 (keep the CLI guard; accept `./` and absolute paths; redact first) and 12 (optional sessions/states). Repair round 1 of 2 goes to a fresh implementer: Decisions 10–12, plant one not-80 group set before the pre-rest in the generator, delete the `currentAt` −1 fallback. The EVAL.md label schema note (finding 6) is an orchestrator doc follow-up.
- 2026-09-25, repair implementer (round 1): items 1–5 are done. Tests were written first for 1 and 4 and recorded failing. The not-80 mutation was caught. PASS: obd-battery 16/16, pnpm check, recordings 20/20, git diff --check, the generator and the artifact byte-identical over two runs (fixture `9539d685…`, label `f99514b0…`, artifact `5d9007eb…`). Deviation: the not-80 set is planted in a new 5 s cycle, so the synthetic counts go from 355 to 356; the real section is unchanged. The path guard is checked through the CLI and the optional fields through the typecheck. Next action: re-review.
- 2026-09-25, Stage A re-reviewer: **REQUEST_CHANGES**, 1 blocking finding, caused by the wording of Decision 10. The post-rest is clipped to the last sample ≤ 1800 s and then required to be ≥ 1800 s, so a complete log fails when its timestamps are 4 ms off (reproduced). Minor: a dead `not80` throw in the generator, a dead `after === 0` branch in `currentAt`, and the spec not noting the 5 s cycle. Items 2–5 of round 1 are confirmed fixed; items 2 and 3 need no tests.
- 2026-09-25, orchestrator: Decision 10 reworded (the gate tests the uncut rest run) and the spec notes the 5 s cycle. Repair round 2 of 2 (the last) goes to a fresh implementer.
- 2026-09-25, repair implementer (round 2): the gate now tests the uncut run `postRestRun`. −4 ms shift PASS (it failed before the fix, and the mutation is caught); t<3510 FAIL; the dead generator throw is removed. The `currentAt` `after === 0` branch is kept because it is reachable (shown in a scratch replay). There is no +4 ms case: the synthetic 10 s cadence turns +4 ms into a 10.004 s gap above MAX_GAP_S, so the fail there comes from the gap rule, not the clip. Real logs use a 5 s cycle (Decision 3). PASS: obd-battery 18/18, pnpm check, recordings 20/20, git diff --check, generator and artifact byte-identical (`5d9007eb…` unchanged). Next action: final re-review.
- 2026-09-25, final reviewer: **APPROVE**. It reproduced the −4 ms PASS and the t<1720/1760/3510 FAILs, confirmed the +4 ms gap argument and the gap rule (runs continue while span ≤ 10 s; the gate fails when span > 10 s), confirmed the `after === 0` branch is reachable, and caught 3 mutations. pnpm check, recordings 20/20, generator and artifact byte-identical. One minor follow-up: when a gap past the clipped window splits the post-rest run, the Gate line reports only the short rest and not the gap; the Samples line still shows the gap.
- Stage A closed and committed by path. PLAN T2.4 stays open: B1 waits for Codex's T2.6 commits, B2 for `App.tsx`, and C (the real charge log) is hardware-only.
- Doc follow-ups (orchestrator, after Codex commits): the EVAL.md label schema (`car: "none"`, `reference.method: "synthetic-generator"`, extra keys), plus ARCHITECTURE.md and the fixtures/README inventory.
- 2026-09-25, orchestrator: Codex committed T2.6 (`80853ab`), so B1 is unblocked. B1 implementer spawned. T2.9 Stage A runs in parallel in disjoint obd-core files.
- 2026-09-25, B1 implementer: done, not committed. The tests were written first and recorded failing (11). PASS: all 12 failure modes, 7 mutations caught, mobile 114/114, typecheck, pnpm check (obd-core 234, battery 18, mobile 114, relay 58, bridge), git diff --check, expo export, a Metro bundle of the logger through a scratch entry, and the first-write rule after every boundary. Hardware NOT RUN; none is needed in B1.
- 2026-09-25, orchestrator: Decision 13 adds the boundary reason `elm-error`; Decision 14 accepts the widened `meta()` and deviations 3–7. Next action: reviewer.
- 2026-09-25, B1 reviewer: **REQUEST_CHANGES**. Blocking: live sample timestamps are taken before a core retry, so the phone can stop "complete" while the replay gate FAILs at 1799.48 s (reproduced with one CAN ERROR on the first post-rest `2414`). Minor: `meta()` record can override `t`/`dir`; non-ELM errors are treated as a disconnect; `link.transport` is dead; the bundle doesn't include the logger until B2. pnpm check PASS, 9/9 reviewer mutations caught, artifacts byte-identical, the happy artifact replays to Gate PASS.
- 2026-09-25, orchestrator: Decisions 15 (the sample t is the answered attempt's tx t) and 16 (`meta()` guard, error classification, B2 re-export), and failure mode 13 `retry-timestamps` added to the B1 list. Repair round 1 of 2 goes to a fresh implementer.
- 2026-09-25, B1 repair implementer (round 1):
  - Decision 15: sample t is the answered attempt's tx t via `RecordingBuffer.tx()`; the silent-stop and wait-for-charge checks use `elapsed()`.
  - Decision 16: `meta()` guard; `LinkError`-only disconnect; non-link errors stop the run as partial.
  - Tests 13 and 14 failed before the fix; mutations M1–M4 caught.
  - PASS: mobile 116/116, typecheck, pnpm check, git diff --check, happy and retry artifacts byte-identical and Gate PASS on replay, expo export.
- 2026-09-25, orchestrator: failure mode 14 added to the spec. Open for the reviewer: the final "Charge log stopped" `onStatus` call is unguarded after the file is saved. Next action: re-review.
- 2026-09-25, B1 re-reviewer: **REQUEST_CHANGES**. Blocking: the final `onStatus` is unguarded, so `runChargeLog` can reject (it breaks the §Interfaces "never rejects" contract that B2 relies on). Major, in Stage A code: the replay adds the failed first attempt of a retried group DID, which drops the open group set (live 565 groups vs replay 564; the group gap doubles to 10 s). All first-review findings are confirmed fixed; pnpm check PASS; artifacts byte-identical.
- 2026-09-25, orchestrator: Decision 17 covers both, and failure mode 13 is extended to retried group DIDs. Repair round 2 of 2, the last, goes to a fresh implementer. It includes the Stage A file `packages/obd-battery/src/session.ts`, because the defect is the replay half of Decision 15.
- 2026-09-25, B1 repair implementer (round 2):
  - Decision 17 done: the final status is guarded, and replay adds only non-error replies (one line in `session.ts`).
  - Both new cases (`retry-group`, `callback-error-final`) were recorded failing first; mutations M1 and M2 caught.
  - PASS: mobile 117/117, obd-battery 18/18, pnpm check, git diff --check, expo export.
  - The Stage A artifact is byte-identical (`5d9007eb…`).
  - The B1 artifacts (happy, retry-timestamps, retry-group) are byte-identical over two runs and replay to Gate PASS.
  - Next action: final re-review.
- 2026-09-25, B1 final reviewer: **REQUEST_CHANGES**, after 2 of 2 repair rounds.
  - Blocking: replay still adds a failed first attempt when core retries a `data` reply with a dropped incomplete/sequence frame (the clone "drop the last line under load" quirk, ELM327.md:210). The open group set closes, and the replay reports false group gaps. Three such cycles give a false Gate FAIL, while the phone stopped complete.
  - Replay-only and recoverable: the answered retry is in the file.
  - The fix was checked in scratch: skip attempts with dropped incomplete/sequence.
  - Everything else PASS: pnpm check, artifacts byte-identical, mutations caught.
- Escalation: the repair limit is reached, so this goes to the owner.
- 2026-09-25, owner: "One more small round". Escalation 1: a third repair round is authorized. Decision 18: replay skips an attempt when the next tx in its segment is the same command (core retried it). Fresh implementer.
- 2026-09-25, B1 repair implementer (round 3, owner-authorized):
  - The Decision 18 structural rule is implemented in `session.ts`. It matches core's single retry. The `dropped`-based alternative would skip the answered second attempt that the phone keeps.
  - The new tests `retry-cf` and `retry-cf-3` failed before the fix. The mutation was caught, and with it in place the replay prints FAIL at a 19.988 s group gap.
  - PASS: pnpm check (mobile 119), obd-battery 18/18.
  - The Stage A artifact is byte-identical (`5d9007eb…`).
  - All B1 artifacts are byte-identical, and their replays pass the gate.
  - Next action: final review.
- 2026-09-25, B1 final reviewer (after round 3): **APPROVE**.
  - It reproduced pnpm check, the Stage A artifact (`5d9007eb…`), 20 B1 artifacts byte-identical with the gate passing on replay, 5 caught mutations, and live == replay on every retry probe.
  - Minor test gaps (non-blocking): Decision 18 vs the dropped-based rule has no pinning test (probe H2 shows they differ); the final-error `kind` check has no test; the `meta` guard has no test.
  - **Major, pre-existing policy gap (must fix before Stage C):** one timeout recovery during the post-charge rest leaves a current gap of 10.7–15.9 s, which is over MAX_GAP_S. That splits the rest run, and no rule stops the logger, so it polls until the 16 h limit and can never pass. Reproduced (probes K and L). The next step is a spec decision (B2 or a small B1 follow-up) before the owner's charge session.
- B1 is closed and committed by path. The 2026-09-24 fixtures were excluded.
- 2026-09-25, owner: "Allow recovery gaps ≤ 20 s". Spec Decision 19 records the rule. The PLAN T2.4 verify line is amended. The follow-up is called B1b: implement Decision 19 in `session.ts`, `capacity.ts`, `charge-log.ts`, the live stop, and the tests and artifacts. B2 (the foreground service, App.tsx, the new dependency) runs in parallel on disjoint files.
