# T0.6b: Android phone relay mode

## Goal

Add a foreground relay mode to the existing Android BLE console. After the user connects to the Veepeak, they can enter the WSL2 relay URL and per-process token, connect the app's built-in WebSocket, and forward one server-authorized ELM command at a time over the existing `BleTransport`. The app independently applies T0.6a's policy, waits for a complete `>` response, reports the exact bytes and elapsed time, rejects malformed or concurrent requests, and presents an explicit native confirmation on the phone for each exact Mode 04 request before any BLE write.

T0.6a is an implementation prerequisite. Together the two specs satisfy T0.6; the owner-arranged HIL gate remains the final hardware evidence.

## Non-goals

- No MCP SDK or MCP protocol inside the app. The phone speaks only the small authenticated WebSocket protocol defined by T0.6a; MCP stays in Node.
- No new npm or native dependency, EAS config/plugin change, APK rebuild, background service, screen-lock prevention, reconnect loop, TLS, QR scanner, token persistence, secure-store work, navigation framework, or production UI.
- No local relay recording buffer or file export. The Node relay owns start/stop and the raw original; the existing manual console recording/export stays available and unchanged.
- No vehicle profile, signal display, DTC clear button, generic arbitrary-command UI, or automatic Mode 04 approval. Confirmation is per request and cannot be remembered.
- No UDS writes (`2E`, `31`, `2F`), session changes, unknown services, unknown AT commands, or arbitrary binary traffic, even if a malformed or compromised relay requests them.
- No simultaneous manual console recording/session and relay mode. One BLE transport has one response reader at a time.
- No edits to recordings, the Node relay spec/implementation, planning/decision/task-run documents, native configuration, or the pre-existing T0.8c Metro/color baseline except where `App.tsx` must integrate relay controls.

## Interfaces

All new exports are named.

### Relay client

`apps/mobile/src/relay/RelayClient.ts`:

```ts
import type { Transport } from "obd-core/transport";

export interface RelayWebSocket {
  readonly readyState: number;
  send(data: string): void;
  close(code?: number, reason?: string): void;
  addEventListener(type: "open" | "message" | "error" | "close", listener: (event: unknown) => void): void;
  removeEventListener(type: "open" | "message" | "error" | "close", listener: (event: unknown) => void): void;
}

export interface RelayPhoneMeta {
  vehicle: "chevrolet-equinox-ev-2024";
  dongle: "veepeak-obdcheck-ble";
  writeChar: string;
  notifyChar: string;
  mtu: number;
}

export type ConfirmMode04 = () => Promise<boolean>;

export class RelayClient {
  constructor(
    socket: RelayWebSocket,
    transport: Transport,
    meta: RelayPhoneMeta,
    confirmMode04: ConfirmMode04,
    timeoutMs?: number,
    nowMs?: () => number,
  );
  close(): void;
}
```

Behavior:

1. On WebSocket open, send the exact T0.6a `hello` with `protocol: 1` and BLE metadata. Reject/close on invalid URL setup before construction; never log, display in the transcript, or persist the token after it has been used to construct the URL.
2. Validate each inbound JSON object and permit one `command` request at a time. Call `authorizeRelayCommand(command)` from `obd-core/transport/relay` independently. Reject duplicate IDs, a second pending request, invalid JSON/schema, binary/non-string messages, pre-open traffic, or any policy mismatch without writing BLE.
3. For a normal allowed command, require `confirmMode04 === false`. For exact `04`, require `confirmMode04 === true`, then await the injected confirmation callback. Denial sends `confirmation-denied` and performs no BLE write. Approval applies only to that request; send exactly `04\r` once. A true flag on any other command, or false on `04`, is `invalid-command`.
4. Subscribe once to `BleTransport`, create a fresh `ElmLineReader`, send `{ type: "started", id }` immediately before calling `transport.write()` with the normalized command plus one `\r`, collect notification chunks byte-for-byte, and settle only at the first `>` prompt. Return all bytes through and including that prompt as standard base64 and elapsed monotonic milliseconds. A second completed response or bytes after completion are not attached to a later request; close the relay as a protocol/session error because response ownership is ambiguous. A denied/invalid command never sends `started`; a write failure after `started` returns `transport-error`.
5. Default command timeout is 20 seconds, matching the existing foreground console. On timeout, transport write failure, socket send failure, socket close/error, BLE disconnect, or `close()`, reject/clean the one pending request exactly once, unsubscribe, reset the reader, and prevent late callbacks from settling a later request. `close()` is idempotent and does not close the shared BLE transport; `App` owns BLE teardown.

The implementation may use the platform `WebSocket` through a small adapter matching `RelayWebSocket`; tests inject a fake. Use `base64-js`, already installed for BLE, for byte/base64 conversion. Do not use `Buffer`, `btoa`, or an assumed UTF-8 decoder for ELM bytes.

### App integration

`App.tsx` adds a compact “Relay mode” section visible only after BLE connection:

- blank editable relay base URL with a `ws://<WSL-host>:8765` placeholder, and a secure-text token field;
- Connect relay / Disconnect relay button and a status line with disconnected, connecting, connected, busy, or error state;
- starting relay is disabled while manual recording is active; starting manual recording is disabled while relay is connecting/connected; BLE disconnect tears down relay first;
- relay connect builds `/phone?token=...` with `URL`/`URLSearchParams` or equivalent safe encoding, then clears the token field after the socket opens; the rendered status/transcript never includes the URL query or token;
- each Mode 04 request invokes React Native `Alert.alert` on the physical phone. The message says clearing DTCs also clears freeze-frame data and asks the user to confirm this one operation. Cancel/deny resolves false; the destructive action button resolves true. Dismissing the alert is denial. No timeout, previous answer, MCP argument, or server message counts as approval;
- while the confirmation alert is open, the app remains busy and does not accept another relay command. Leaving relay mode or losing BLE denies/cleans the request;
- manual console behavior, recording export, explicit light/dark colors, and Metro resolution remain intact.

The URL placeholder is application guidance, not an OBD constant. The user enters the WSL2 address printed by `pnpm relay`/`pnpm hil:smoke`; the app does not assume a host address is reachable.

## Files

Three implementation files in one implementer session:

- `apps/mobile/src/relay/RelayClient.ts` — create — strict wire validation, independent policy check, one-command BLE bridge, response framing, confirmation callback, and cleanup.
- `apps/mobile/test/relay.test.ts` — create — fake WebSocket/transport/clock tests for protocol, policy, confirmation, timing, single-flight, response bytes, and disconnect cleanup.
- `apps/mobile/App.tsx` — modify atop the pre-existing approved T0.8c baseline — foreground relay controls, WebSocket lifecycle, mutual exclusion with manual console recording, and native Mode 04 alert.

No package or lockfile change is permitted. If the platform types do not fit the adapter without a dependency or native/config change, stop and return to architecture.

## Dependencies

No new dependency. React Native provides the phone WebSocket and `Alert`; `base64-js`, `obd-core`, React, and React Native are already locked in `apps/mobile/package.json`. T0.6a already exports the shared policy from `obd-core/transport/relay`.

## Sources

| Constant / behavior | Source |
|---|---|
| Commands end in `\r`; completion is `>`; one command in flight | `docs/ELM327.md` §Framing and §BLE specifics |
| Exact read/AT/header allowlist and Mode 04 classification | Ordered prerequisite `docs/specs/T0.6a-node-relay-mcp.md` Sources and Interfaces; the implementation imports `authorizeRelayCommand` rather than copying constants |
| Mode 04 clears DTCs and freeze frame and requires explicit confirmation | `docs/ELM327.md` §Standard modes used; `AGENTS.md` hard rule 5; `docs/DECISIONS.md` ADR-013 |
| UDS writes `2E`, `31`, `2F` and unknown commands must never reach the car | `AGENTS.md` hard rule 5; `docs/ARCHITECTURE.md` §tools/relay; `docs/DECISIONS.md` ADR-013 |
| Phone opens the outbound WebSocket; React Native has built-in WebSocket; app stays foreground/screen on | `docs/DECISIONS.md` ADR-013 |
| Existing BLE transport, discovered characteristic roles, negotiated MTU, and manual console timeout/ownership | `apps/mobile/src/ble/BleTransport.ts`; `apps/mobile/src/console.ts`; `docs/specs/T0.8b-ble-console-recording.md` |
| Equinox is the required Phase 0 vehicle | `docs/DECISIONS.md` ADR-015 |
| Raw relay recording stays local and only script-redacted copies may be committed | `docs/DECISIONS.md` ADR-017; ordered T0.6a recording contract |

No new OBD constant, PID, DID, header, scaling formula, or decode is introduced in this spec.

## Verification

Run from the repository root unless stated otherwise.

### Mandatory local checks

- [ ] `pnpm -F mobile test` runs `apps/mobile/test/relay.test.ts` and passes without real sockets or BLE.
- [ ] The happy-path fake test opens, emits exactly one hello, receives `0100`, sends `started` immediately before writing exactly `0100\r`, remains pending across split notification chunks, and returns standard base64 decoding to the exact bytes through `>` with the injected elapsed milliseconds.
- [ ] Table tests feed all T0.6a policy rejections, especially `2E`, `31`, `2F`, `10`, unknown AT, `04FF`, CR/LF/tab/NUL smuggling, and two-command input. Each returns `invalid-command` and leaves transport writes empty even when the fake server sets a confirmation flag.
- [ ] Mode 04 tests prove: false/missing confirmation flag is rejected; true flag calls the callback; denial/dismissal sends no `started`, writes nothing, and returns `confirmation-denied`; approval sends `started` then writes exactly one `04\r`; approval is requested again for the next Mode 04; a true flag on `0100` is rejected.
- [ ] Lifecycle tests cover pre-open request, duplicate/request-while-busy, timeout, write rejection, socket send failure, socket error/close, explicit close, and BLE-side close notification supplied by `App`. Each pending promise/result settles once, unsubscribes/resets, and late data cannot satisfy another request.
- [ ] `App.tsx` typecheck proves relay/manual recording mutual exclusion and teardown ownership. A source assertion or component-light test verifies token state is cleared after open and no rendered status/transcript includes the query/token; do not add a React Native rendering-test dependency solely for this.
- [ ] `pnpm -F mobile typecheck`, `pnpm -F mobile test`, and `pnpm check` pass.
- [ ] `cd apps/mobile && pnpm dlx expo-doctor@1.20.4` reports all checks passed/no issues, confirming no dependency or native configuration drift.
- [ ] `cd apps/mobile && pnpm exec expo export --platform android --output-dir <fresh-directory-under-/tmp>` exits 0 using the existing Metro resolver; nothing is written inside the repository.
- [ ] `git diff --check` passes. `git diff -- apps/mobile/package.json apps/mobile/app.json apps/mobile/eas.json pnpm-lock.yaml` is empty. Review distinguishes the pre-existing approved T0.8c changes in `App.tsx`/`metro.config.js` from this spec's `App.tsx` additions.

### Physical phone integration, mandatory for T0.6 hardware acceptance

Prerequisites: the pending T0.8 Equinox phone acceptance is completed or rerun; the existing development client is installed; Metro loads this JavaScript-only change; phone and WSL2 can reach each other; no dependency/plugin change requires an APK rebuild.

1. With no dongle connection, attempt relay connect and verify status is clear and no crash occurs.
2. Connect the Veepeak on the Equinox in Ready/Park. From WSL2 run `pnpm hil:smoke`, enter its printed URL and token on the phone, and tap Connect relay.
3. Keep the app foregrounded with the screen on. Let the smoke send `0100`; verify the app shows relay busy then connected again and the WSL2 command passes.
4. In a separate `pnpm relay`/MCP test while the owner is present, request a known rejected `2E...` command and confirm the phone performs no BLE write. Request exact `04`; tap Cancel and confirm no BLE write and an MCP error. **Do not approve Mode 04 merely to test it.** Approval is supported by automated fake-transport tests; an actual clear is unnecessary and could destroy diagnostic evidence.
5. While a harmless read is pending, disconnect relay or BLE and verify WSL2 receives a prompt disconnected failure and the app can reconnect cleanly.

- [ ] Record phone model/Android version, Metro mode, relay URL host class (LAN/mirrored, without token/query), Equinox state, and the T0.6a HIL recording paths/hashes in `docs/task-runs/T0.6.md`.
- [ ] Mark real Mode 04 approval **NOT RUN (not required; destructive)** unless the owner separately needed to clear codes and explicitly approved it on the phone. A denial is the required physical safety check.

The physical integration and T0.6a HIL check are **NOT RUN** until the owner arranges the pending session. Automated tests cannot promote this to hardware PASS.

## Risks / open questions

- **Largest risk: lifecycle ownership between the manual console, relay reader, BLE disconnect, and WebSocket close.** Two readers would assign one dongle response to the wrong caller. `App` must make the modes mutually exclusive and tear down relay synchronously before reusing the transport.
- React Native's WebSocket event typings differ from DOM typings. The small injected adapter isolates that difference. If Expo SDK 57 lacks a needed event behavior on the physical phone, report the exact behavior and return to architecture; do not add a socket package.
- The phone must remain foregrounded with its screen on, as ADR-013 records. Background relay work belongs with later foreground-service scope.
- The relay token is transient but entered in UI. Clearing it after open and never copying the query into status/transcript reduces accidental disclosure; transport encryption remains outside this local-only task.

No material design question remains open. T0.6a fixes the wire and policy; this spec uses the existing Android stack and requires no dependency or native-build choice.

## Decisions

- Keep the Android half separate and ordered after T0.6a so the wire, server policy, and fake phone are stable before UI integration.
- Reuse `BleTransport` directly with a dedicated relay response reader. Manual console recording and relay mode are mutually exclusive rather than sharing response buffers.
- Apply the shared allowlist on both server and phone. The server remains authoritative for MCP, while the phone refuses unsafe traffic if the network peer is malformed or compromised.
- Use a per-request native alert for Mode 04. Denial/dismissal is the normal physical acceptance path; no real DTC clear is required.
- Keep relay changes JavaScript-only. The existing development client can load them through Metro once the pending T0.8 acceptance baseline is valid.

Orchestrator note (2026-09-24, T0.9b): Start/Stop recording and Export recording no longer exist. "Mutual exclusion with manual console recording" (Files, Verification) now reads "mutual exclusion with the unsaved debug session and with Run capture / Run codes report" (`docs/specs/T0.9b-one-and-done-captures.md`).
