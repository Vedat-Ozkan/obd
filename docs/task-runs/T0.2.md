# T0.2 task run

## Ownership

- Last tool: Codex
- Last role: orchestrator (ADR-015 scope reconciliation after Claude reviewer round 2 APPROVE)
- Current stage: software half reviewed; EV recording and Gate A reconciliation pending. ADR-015 superseded the ICE and bridge hardware requirements; T0.2 remains unticked until the revised EV evidence and spike document are reconciled.
- Completed stages: preflight, architecture, scope confirmation (user approved 2026-09-20), implementation (software half), repair round 1, review round 2 (APPROVE)
- Other active tool or role: none

## Contract

- Task: T0.2 — hardware spike (no product code): raw ELM327 transcripts on all three cars, spike doc, Phase 2 go/no-go
- Spec paths: `docs/specs/T0.2-hardware-spike.md`
- Approved decisions: four open questions answered by the user 2026-09-20, recorded in the spec Decisions section (ATI sourced from PLAN; user checks OBDb upstream before car session; verbatim spacing; ICE engine off / EV ready)
- Repair count: 1 of 2 (used)
- Escalation count: 0
- Escalation reason: none

## Baseline

- Git state at start: clean, `main` at `2a75c68` in sync with `origin/main`; `git status --short` empty
- Existing specs: `docs/specs/README.md`, `T0.1a-typescript-workspace.md`, `T0.1b-hil-bridge-ci.md`, `X-2026-09-16-codex-workflow.md`
- `fixtures/recordings/` and `fixtures/synthetic/` are empty (untracked, invisible to git)
- Tooling: see preflight output in this session; the HIL bridge and any bleak script run on the laptop near the car, not in this WSL2 shell
- Constraint: the car/laptop session is not available in this shell; hardware items will be NOT RUN until the user runs them

## Touched files

- `docs/task-runs/T0.2.md` (this record)
- `docs/specs/T0.2-hardware-spike.md` (architect; Decisions filled by orchestrator)
- `tools/spike/spike.py` (implementer, created, 142 lines)
- `tools/spike/test_spike.py` (implementer, created)
- `docs/spike-2026-09.md` (implementer, created skeleton; all cells NOT RUN)

## Verification evidence

### Implementation (Claude implementer, 2026-09-20)

- `cd tools/hil-bridge && uv run ruff check ../spike` — PASS (ruff 0.16.8; `# noqa: SIM115` on the long-lived Recorder handle)
- `uv run --no-project --with pytest pytest tools/spike` — PASS (1 passed)
- `uv run tools/spike/spike.py --help` — PASS (inline metadata resolves; bleak 3.0.2 in ephemeral env; no Bluetooth touched)
- Script limits (<150 lines, no `04`, only four DIDs, no decoding) — PASS (142 lines)
- `docs/spike-2026-09.md` skeleton (sections 1–6, NOT RUN cells, NOT DECIDED, criteria verbatim) — PASS
- `pnpm check` — PASS (exit 0)
- `git status --short` — PASS: only spec, spike doc, task record, spike.py, test_spike.py
- Stub-bleak dry run of `main()` (scratchpad only, synthetic, not hardware evidence) — PASS
- All Laptop items (`--scan`, three car recordings, recordings parse, bridge reachability from WSL2, spike doc sections 1–6 filled, go/no-go) — NOT RUN: no dongle/car in this shell
- `pnpm hil:smoke` — NOT RUN: bridge runs on the laptop near the car

### Repair round 1 (Claude implementer, 2026-09-20)

- Findings 1–4 addressed: `os.makedirs(..., exist_ok=True)` before the mode-"x" open (spike.py now 145 lines); timeout/misattribution sentence in the spike doc How-to-run; `18DAF1CB` expected header on the three DACB rows; section 2 Result cell names PASS / FAIL / NOT RUN. Test now writes into a not-yet-existing subdir (covers finding 1).
- `uv run ruff check ../spike` — PASS; `uv run --no-project --with pytest pytest tools/spike` — PASS (1 passed); `--help` — PASS; `wc -l` 145 — PASS; `pnpm check` — PASS; `git status --short` — PASS (five expected paths)
- Laptop items — NOT RUN (unchanged)

Round-0 implementer deviations flagged: script does not `mkdir -p` the recording parent dir (fresh clone lacks `fixtures/recordings/<car>/`); `FileExistsError` traceback on overwrite rather than argparse error; cosmetic header/stderr hint.

## Review findings

### Round 1 (Claude reviewer, 2026-09-20): REQUEST_CHANGES

Reviewer reran all seven "Here" items: PASS. Eight Laptop items NOT RUN (no Bluetooth/dongle/car in this shell). Constants audit: all sourced, transcriptions exact; no `04`, no UDS writes. Recording format matches ARCHITECTURE.md.

1. [blocking, open] `tools/spike/spike.py:45` — parent dir of `--out` not created; fresh clone lacks `fixtures/recordings/<car>/`, so every first car run fails with FileNotFoundError. Fix: `os.makedirs(os.path.dirname(path) or ".", exist_ok=True)` in `Recorder.__init__` (portable across Windows/Linux; keeps mode "x"). Accepted deviation from the spec's "internal structure, no more".
2. [minor, open] After a `--timeout`, a late `>` can under-report the next printed row; add a sentence to the spike doc How-to-run block that the recording is the authority and an `rx` may precede a `tx`.
3. [minor, open] `docs/spike-2026-09.md` DACB rows lack the expected reply header `18DAF1CB` needed by the GO criterion.
4. [minor, open] `docs/spike-2026-09.md` section 2 Result cell should name the allowed values PASS / FAIL / NOT RUN.
5. [informational, no change] `mtu_size` may be the BlueZ default 23; T0.3 must not treat the `mtu` meta key as authoritative.
6–8. [no change] `# noqa: SIM115` justified; header row / stderr hint harmless; FileExistsError on overwrite satisfies "refuses to overwrite".

### Round 2 (Claude reviewer, 2026-09-20): APPROVE (software half)

- Fixes 1–4 re-verified present and correct; line count 142→145 accounts for exactly the three script lines; test exercises the makedirs path.
- All seven "Here" items rerun by the reviewer: PASS. Constants audit rechecked: all sourced (ELM327.md line refs recorded in the reviewer report); no `04`, no UDS writes, no decoding.
- Open, informational only: `mtu` meta key may be the BlueZ/WinRT default 23 (T0.3 must not treat it as authoritative); after a `--timeout`, the printed row for the next command may be under-reported; the recording is authoritative.
- Findings 1–4 from round 1: CLOSED.

## Blocker

- Hardware: no Bluetooth, dongle, car, or laptop bridge reachable from this WSL2 shell. The user must run the car session on the laptop.

## Prep for a short laptop session (Claude, 2026-09-22)

- OBDb check (spec Decision 2): DONE by Claude against upstream commit `15ee122`; commands and module ids match `EV_COMMANDS`; no script change. Upstream 2024 test data: `DACB` `27C6`/`2AF5`/`2B43` supported, `DA1D` `33E5` unsupported, `33E5` ≤25.5 V (not HV pack). Recorded in the spec Decisions, ELM327.md, FEASIBILITY.md.
- Scope reduced with the user's constraint (little laptop time, EV first): EV recording + Gate A required; ICE recordings optional here, otherwise from the phone console after T0.8; bridge reachability dropped (ADR-013). PLAN.md T0.2 verify line amended.
- `docs/spike-2026-09.md` gained an "EV-first quick run" card; the user brings back only the `.jsonl` plus its SHA-256; Claude fills the tables from the recording.

## Next action

- ADR-015 (2026-09-23) supersedes the earlier three-car and bridge instructions above. Reconcile the tracked Equinox spike recordings with `docs/spike-2026-09.md`: fill the EV per-command table and Gate A from recording evidence, then independently verify the EV files parse and replay. Do not treat unrun Chrysler or Elantra rows as a blocker or hardware PASS.
- T0.2 remains unticked until that EV-only reconciliation and review are complete. The historical review reports and original spec are preserved.
