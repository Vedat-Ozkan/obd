# T0.8d task run

## Ownership

- Last tool: Claude
- Last role: reviewer (round 1: APPROVE, 2026-09-24)
- Current stage: CLOSED 2026-09-24 (fixture-side reviewed; hardware capture run by the owner and replayed green)
- Completed stages: preflight, architecture (Claude architect 2026-09-23; spec complete on disk although the agent was cut off by the API session limit before reporting), decisions (orchestrator under owner delegation, 2026-09-24)
- Other active tool or role: none on `apps/mobile` (Codex's T0.8c is closed and approved, uncommitted); Claude's X-2026-09-23-write-safety review is running on `packages/obd-core` and `docs/ELM327.md`

## Contract

- Task: T0.8d — one-button acceptance capture in the phone app (owner request 2026-09-23: "I connect to my Veepeak, I press start recording, it does everything on its own and stops and exports, all by itself"). After connecting, one button starts a recording, sends a fixed, sourced command sequence one at a time through the existing `ConsoleSession` (waiting for each `>`; continuing past errors and timeouts, which are recorded), stops the recording, and exports it (save plus automatically opening the share sheet). Sequence for the T0.8 Equinox acceptance: `ATZ ATE0 ATL0 ATS0 ATH1 ATSP0 0100`, plus `020000` and `020200` for T0.5 Stage C (T0.5 Decision 2). The manual console stays available.
- Spec paths: `docs/specs/T0.8d-one-button-capture.md`
- Approved decisions: owner request 2026-09-23 (above); spec open questions 1–4 decided by the orchestrator 2026-09-24 (spec Decisions): add `03`; stale-until-prompt in the console; app-side `json.dumps` form; prefilled default note
- Repair count: 0 of 2
- Escalation count: 0
- Escalation reason: none

## Baseline

- Git state at start: `main` at `f22c970`. Uncommitted: Codex T0.8c (approved; `apps/mobile/App.tsx`, `apps/mobile/metro.config.js`, `docs/specs/T0.8c-mobile-follow-up.md`, `docs/task-runs/T0.8.md`); Claude write-safety task in review (`docs/ELM327.md`, `docs/ARCHITECTURE.md`, `packages/obd-core/...`, its spec and record).
- App: `apps/mobile/src/console.ts` (read-only allowlist, single-flight, tx recorded before write), `src/recording.ts`, `src/ble/BleTransport.ts`, `App.tsx` with Start/Stop recording and Export.
- Installed dev-client APK from EAS build `0f877dca` is valid for JS-only changes.

## Touched files

- `docs/task-runs/T0.8d.md` (this record)
- `docs/specs/T0.8d-one-button-capture.md` (architect; Decisions by orchestrator)
- `apps/mobile/src/capture.ts` (new; implementer)
- `apps/mobile/src/console.ts` (stale-until-prompt, `closed`, `waitForPrompt`; implementer)
- `apps/mobile/src/recording.ts` (`toJsonl()` in Python `json.dumps` form; implementer)
- `apps/mobile/App.tsx` (Run capture, progress lines, `exportJsonl(jsonl)`, freeze returns JSONL, teardown meta, prefilled note; implementer)
- `apps/mobile/test/capture.test.ts` (new; C1-C7)
- `apps/mobile/test/console-recording.test.ts` (two existing tests updated with a late `>`; S1-S3, R1 added)

## Verification evidence

Implementer, Claude, 2026-09-24:

- Tests first: C1-C7, S1-S3, R1 written before the code. On the pre-change source `capture.test.ts` failed to import `../src/capture.js`; S1 (timed out: the send wrote), S2 (`waitForPrompt` not a function), S3 (`closed` undefined), R1 (compact `JSON.stringify` line) failed. The two existing tests ("timeout records the meta note…", "write failure…") both still existed and were updated to send a late `>` first; they pass before and after.
- `pnpm -F mobile test`: 3 files, 57 tests PASS (includes `test/capture.test.ts`, 7 tests).
- Mutation checks on a temp copy outside the repo (deleted): (1) `stale` check removed from `send()` -> S1 fails; C5 does not fail (the runner stops on `waitForPrompt` false before calling `send`, so C5 never reaches the check). (2) runner continues after `waitForPrompt` false -> C5 and C6 fail. (3) runner stops on an error reply -> C3 fails.
- R1 expected text generated with `python3 -c 'import json; print(json.dumps(...))'` (command in the test comment). Python escapes U+007F too, so `toJsonl()` escapes U+007F and above.
- `redact_vin.py` on a capture-shaped `toJsonl()` output (fake transport, spike reply chunks, temp dir outside the repo, deleted): `47 lines`, `no VIN found`, exit 0; `.redacted.jsonl` written beside it.
- `pnpm -F mobile typecheck`, `pnpm lint`, `pnpm check`: PASS (obd-core 134 tests, mobile 57, hil-bridge ruff + pytest). `git diff --check`: clean.
- `pnpm exec expo export --platform android --output-dir <scratchpad>`: exit 0 (one android bundle); `git status --ignored apps/mobile` unchanged; output deleted.
- `git diff -- apps/mobile/package.json apps/mobile/app.json pnpm-lock.yaml`: empty. `pnpm exec expo install --check`: exit 1, reports `expo@57.0.24` (expected ~57.0.25) and `expo-sharing@57.0.21` (expected ~57.0.22). Pre-existing, from upstream patch releases; this task changes no dependency and did not act on it (JS-only constraint).
- Static safety: `rg -n '04|2E|2F|31' apps/mobile/src apps/mobile/App.tsx` matches `console.ts:6` (comment) and `App.tsx:18` (`Platform.Version >= 31`, Android API level for BLE permissions, not an OBD value). The only `transport.write` in `apps/mobile/` is `console.ts` in `send()`, after `normalizeReadOnlyCommand`, the closed check and the stale check (`App.tsx` `file.write` is the export file).
- Hardware (Android + Equinox): NOT RUN; the owner runs it on the phone.


### Equinox phone capture (owner, 2026-09-24; reconciled by the orchestrator)

- Recording: `fixtures/recordings/chevrolet-equinox-ev-2024/2026-09-24-phone-console.jsonl` (local, gitignored; SHA-256 `388fcb71…eaf7`, 53 lines). Committed copy: `2026-09-24-phone-console.redacted.jsonl` from `redact_vin.py` (exit 0, `no VIN found`, 54 lines, SHA-256 `e87dab6c…be3d`). A Windows `:Zone.Identifier` side file from the copy was deleted.
- `pnpm replay` on both: exit 0, `summary: 10 commands; data 5, ok 5, nodata 0, error 0, timeout 0; frames 12; dropped none`. `0100` (L18, after `SEARCHING...`) answered by 45, 40, 17, CB, 28 with the same bitmaps as the 2026-09-22 spike. `03` (L46): all five `dtcs none`.
- Mode 02 (for T0.5 Stage C): only module 17 answered. `020000` (L38) → `42 00 00 40 08 00 03`; `020200` (L42) → `42 02 00 00 00`. The other modules sent nothing. Not decoded here.
- `pnpm -F obd-core test` PASS (136; the recordings test picks up the new file).

## Review findings

### Round 1 (Claude reviewer, 2026-09-24): APPROVE

- 7/7 fixture-only items. Reproduced: `pnpm check` PASS (obd-core 134, mobile 57); mutations (1) S1 only (Decision 5), (2) C5+C6, (3) C3; R1 byte-identical to `python3 json.dumps`; `redact_vin.py` on a capture-shaped export exit 0 `no VIN found`; `expo export` exit 0; no package/app/lockfile diff; the only `transport.write` (console.ts:76) comes after the allowlist, closed and stale checks; export uses the frozen string (`endRecording() ?? frozenRef.current`); Disconnect enabled during capture; constants sourced (0100/03 chunks verbatim from spike lines 25–41, 107–114). All deviations accepted.
- 1. [minor, open, optional] `App.tsx:143-150`: a disconnect between steps reports `Capture stopped at step 7 of 10 (0100): disconnected`, naming the last step reached, not a failed one. Recording correct. Wording fix at next touch.

## Blocker

- None.

## Next action

- Owner commits (T0.8c + T0.8d together; apps/mobile test pruning is the owner's own change).
- Owner runs hardware steps 1–8 on the Equinox (spec §Verification). Record the file path, replay summary, `redact_vin.py` result, and whether `020000`/`020200` got a positive `42` reply (for T0.5 Stage C). Acceptance for the T0.8b gate is ruled after that run.
