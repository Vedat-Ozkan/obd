# T0.3 task run

## Ownership

- Last tool: Codex
- Last role: orchestrator (closure after follow-up review APPROVE, 2026-09-23)
- Current stage: CLOSED for software 2026-09-23; ICE recording replay HARDWARE PENDING until T0.8
- Completed stages: preflight, architecture, scope confirmation (user approved 2026-09-21), implementation, review round 1 (APPROVE 2026-09-23), minor test follow-up, independent follow-up review (APPROVE 2026-09-23)
- Other active tool or role: none

## Contract

- Task: T0.3 — `obd-core`: `Transport` interface, `ReplayTransport` over the recording format, `ElmLineReader` (accumulate until `>`, strip echo, split lines, classify `NO DATA` / `SEARCHING...` / errors)
- Spec paths: `docs/specs/T0.3-transport-replay-linereader.md`
- Approved decisions: two open questions answered by the user 2026-09-21, recorded in the spec Decisions section (undocumented ELM strings stay unclassified; synthetic framing fixture uses a meta flag, no label file)
- Repair count: 0 of 2
- Escalation count: 0
- Escalation reason: none

## Baseline

- Git state at start: `main` at `2a75c68`; untracked T0.2 software half awaiting the user's commit: `docs/specs/T0.2-hardware-spike.md`, `docs/spike-2026-09.md`, `docs/task-runs/T0.2.md`, `tools/spike/`. These are not part of T0.3.
- `packages/obd-core/` contains only `package.json` (no `src/`); `fixtures/recordings/` is empty — no T0.2 recordings exist yet (T0.2 hardware half pending)
- Constraint: the PLAN verify line ("unit tests against T0.2 recordings") cannot be fully met until the recordings land; the spec must separate synthetic response-class tests (runnable now) from recording-backed tests (NOT RUN until T0.2 hardware half)
- Tooling: Node v22.13.0, pnpm 9.15.9, Python 3.10.12, uv 0.12.15

## Touched files

- `docs/task-runs/T0.3.md` (this record)
- `docs/specs/T0.3-transport-replay-linereader.md` (architect; Decisions filled by orchestrator)
- Modified (implementer): `package.json`, `packages/obd-core/package.json`, `pnpm-lock.yaml`, `tsconfig.json`
- Created (implementer): `packages/obd-core/src/transport/types.ts`, `src/recording/format.ts`, `src/transport/replay.ts`, `src/elm/errors.ts`, `src/elm/reader.ts`, `test/replay.test.ts`, `test/reader.test.ts`, `test/recordings.test.ts`, `fixtures/synthetic/elm-framing.jsonl`
- Follow-up (2026-09-23): `packages/obd-core/test/reader.test.ts` (minor finding 1); `docs/PLAN.md` and this record (closure)

## Verification evidence

### Implementation (Claude implementer, 2026-09-21)

- `pnpm install` — PASS (lockfile adds `@types/node@22.20.4`, `undici-types@6.21.0`, `zod@4.6.5`; vite/vitest snapshots re-keyed for the optional peer)
- `pnpm -F obd-core test` — PASS: 35 passed, 1 skipped; skip visible as `↓ fixtures/recordings > NOT RUN: fixtures/recordings/ has no .jsonl files (T0.2 hardware half pending)`
- `pnpm typecheck` — PASS (no `allowJs`)
- Purity grep on `packages/obd-core/src` (`node:`, fs, path, react-native, fetch, Buffer, process) — PASS (nothing)
- `pnpm check` — PASS
- `git status --short` — PASS: the 13 spec files plus T0.3 spec/record; T0.2 untracked files untouched; `fixtures/recordings/` untouched
- Fixture `fixtures/synthetic/elm-framing.jsonl` byte-identical to the spec block — PASS (diff)
- Recording-backed replay over `fixtures/recordings/**` — NOT RUN: no recordings (T0.2 hardware half pending); no code change expected when they land
- `pnpm hil:smoke` — NOT RUN: not a T0.3 item

Implementer deviations for the reviewer: (1) `tsconfig.json` gained `"types": ["node"]` because TS 6.0.3 no longer auto-includes `@types/*` (spec said "nothing else" for tsconfig); alternative is per-file `/// <reference types="node" />` in the two test files. (2) Dependency specifiers as `pnpm add` resolved them (`^22.20.4`, `^4.6.5`). (3) Reader test 21 folded into the shared `parse()` helper. Open design note: `ReplayTransport.write` delivers via `queueMicrotask` (before the caller's `await` continuation); T0.4 may want `setTimeout(0)`.

## Review findings

- See round 1 and the follow-up review below. Only the cosmetic `format.ts` readability finding remains open; ICE replay is pending hardware evidence.

## Blocker

- None.

## Reconciliation (Claude orchestrator, 2026-09-23)

- The T0.3 implementation was committed in `5919fa3` ("update purpose", by the user) with the other docs; no approval existed at that point.
- Since then, T2.3a (`4a735b1`) and T2.3b (`950d320`) changed `packages/obd-core/test/recordings.test.ts` under their own approved specs: a 120 s per-test timeout, `replay()` extracted, an `EARLY_PROMPT` SHA-256-keyed exemption for one line of the 2026-09-23 discovery recording, and three in-memory cases. Those changes are reviewed under T2.3a/T2.3b, not here.
- The previously NOT RUN recording-backed item can now run: `fixtures/recordings/chevrolet-equinox-ev-2024/2026-09-22-spike.jsonl` and `-spike-2.jsonl` are tracked; two discovery recordings are local and gitignored. `pnpm -F obd-core test`: 3 files, 42 passed, 0 skipped (2026-09-23).
- No implementation stage is redone; the reviewer reruns every check.

## Codex follow-up baseline (2026-09-23)

- Owner authorized the T0.3 tick, task records, commit, and push, and asked Codex to decide whether to fix minor finding 1 now. Decision: fix it now because spec verification item 21 already requires the assertion for every case; independently review the changed test before closure. Keep finding 3 as cosmetic and carry the ICE recording check to T0.8.
- At handoff, `git status --short` shows only pre-existing edits to `.codex/agents/{obd_architect,obd_implementer,obd_reviewer}.toml`, `docs/PLAN.md`, and task records T0.3, T2.3a, T2.3b. The agent config edits are outside this closure and must not be staged.
- Permitted implementation file: `packages/obd-core/test/reader.test.ts` only. Spec and existing approval remain; a new reviewer must check the small follow-up and run `pnpm check`.
- The named implementer could not start because the configured `gpt-6-terra` model was unavailable to this account. A fresh `gpt-5.6-terra` fallback changed only `reader.test.ts`, routing completed responses in tests 16–20 through `parse()`; it did not return a report before being stopped. Codex orchestrator inspected the diff and ran `pnpm check`: first run NOT RUN to completion (sandbox blocked uv cache write), escalated run PASS (obd-core 3 files / 42 tests; bridge Ruff and pytest 1 test). This evidence was handed to a fresh reviewer for independent checks.

## Review round 1 (Claude reviewer, 2026-09-23): APPROVE

- `pnpm check` PASS (obd-core 3 files / 42 passed / 0 skipped). Purity grep clean; every ELM response string sourced to docs/ELM327.md §Responses; recordings untouched; synthetic fixture byte-identical to the spec; deps only zod and @types/node; T0.3 hunks in `5919fa3` are exactly the 13 spec files.
- Deviations accepted: (1) `"types": ["node"]` in tsconfig; (2) resolved dependency specifiers; (3) item 21 folded into `parse()` (see finding 1). `queueMicrotask` delivery meets spec rule 3; setTimeout(0) is a T0.4 decision.
- Later T2.3a/T2.3b edits to `recordings.test.ts` do not weaken the check: the exemption is SHA-keyed to the gitignored discovery file only.
- Findings (all minor at round 1): 1. `reader.test.ts:246-293` tests 16–20 did not assert `raw` is untouched (item 21 covered only for 1–15); resolved by the follow-up below. 2. Recording-backed item: only Equinox recordings exist; the Chrysler/Elantra half is NOT RUN (hardware-only, pending T0.8 phone console). 3. `format.ts:8` regex upper bound written as a literal `ÿ` instead of an escape; readability nit.
- Recording-backed verification: PASS for `fixtures/recordings/chevrolet-equinox-ev-2024/2026-09-22-spike.jsonl` and `-spike-2.jsonl` (tracked) plus the two local discovery recordings; Chrysler 200 / Elantra NOT RUN.
- Note for T0.4: real recording shows the ELM clone's garbage byte `ü` after `ATZ` surviving as a content line; discard in the session layer per §Clone quirks.

## Follow-up review (Codex reviewer, 2026-09-23): APPROVE

- Minor finding 1 fixed in `packages/obd-core/test/reader.test.ts`: completed responses in cases 16–20 now pass through the shared `parse()` helper, which asserts `raw` equals its input. The reviewer verified all responses in case 18 are covered, including the buffered tail.
- Reviewer `pnpm check` PASS: TypeScript, ESLint, 42 obd-core tests, bridge Ruff, bridge pytest (1 passed). Reviewer `pnpm -F obd-core exec vitest run test/reader.test.ts --reporter=verbose` PASS (20/20); `pnpm -F obd-core exec vitest run test/recordings.test.ts --reporter=verbose` PASS (8/8); `pnpm install --frozen-lockfile --offline` PASS. Purity grep, fixture/recording diff, and `git diff --check` PASS.
- Recording-backed replay PASS on `fixtures/recordings/chevrolet-equinox-ev-2024/2026-09-22-spike.jsonl`, `fixtures/recordings/chevrolet-equinox-ev-2024/2026-09-22-spike-2.jsonl`, and both local gitignored discovery files. Chrysler 200 and Elantra replay NOT RUN (hardware-only; no recordings yet).
- Blocking and major findings: none. Minor finding 3 remains open: `packages/obd-core/src/recording/format.ts:8` uses a literal `ÿ` in the regex; cosmetic. No source, dependency, or recording changes in the follow-up.

## Next action

- T0.8 phone console: record the Chrysler 200 and Hyundai Elantra baseline command sessions from `docs/PLAN.md` T0.2, export to `fixtures/recordings/chrysler-200-2013/<date>-spike.jsonl` and `fixtures/recordings/hyundai-elantra-2019/<date>-spike.jsonl`, then run `pnpm -F obd-core test -- --reporter=verbose` and record PASS/FAIL here or in T0.8's task record. No ICE hardware verification is claimed yet.
