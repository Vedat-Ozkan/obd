# T0.6 task run

## Current state (as of 2026-09-25)

- **Stage:** T0.6a (relay + car MCP server) closed, committed, and merged to main (`2f3dc2b`, merge `61928f6`) after the final independent APPROVE. T0.6b (Android relay mode) not started; PLAN T0.6 stays open until it and the hardware gate land.
- **Verdicts:** T0.6a review rounds 1–2 REQUEST_CHANGES, round 3 REQUEST_CHANGES at the Codex repair cap (2/2); owner-authorized escalation round 1 used for the shared-write-guard amendment; the two security findings (OBD_RELAY_TOKEN ignored, non-canonical base64) were fixed in an owner-approved round and the re-review returned **APPROVE**. Historical Codex counters preserved, not reset.
- **NOT RUN:** `pnpm hil:smoke` through the relay end-to-end (needs the T0.6b app relay mode and an owner phone/car session); redaction and replay of a relay-recorded file.
- **Open follow-ups for T0.6b:** hil-smoke final `stopRecording()` placement (false-FAIL edge on late disconnect); `docs/ELM327.md` write-safety wording → one-dropped-character model; whitespace-only `OBD_RELAY_TOKEN` still accepted as a token.
- **Next action:** T0.6b per `docs/specs/T0.6b-android-relay-mode.md`, then the owner-arranged hardware session.

## Ownership

- Last tool and role: Codex orchestrator (preflight, 2026-09-23)
- Current stage: architecture
- Completed stages: preflight and worktree isolation
- Other active tool or role on T0.6: none; Claude is working on T0.5 in the main worktree
- Spec paths: pending architect; user requested two ordered specs, relay/allowlist and app relay mode
- Approved decisions: user selected T0.6 for Codex; keep T0.6 and any MCP SDK dependency/lockfile edits on the `codex/t0.6` branch; preserve concurrent T0.5. User confirmed T0.8 Equinox phone acceptance is still pending, so no hardware pass may be inferred.
- Repair count: 0 (new T0.6 task)
- Escalation count and reason: 0; none

## Baseline and scope

- Main workspace `/home/kema/projects/obd` is active for T0.5, with modified `docs/ELM327.md`, `docs/PLAN.md`, `docs/task-runs/T0.4.md`, and untracked T0.5 spec/record/core decoding files at preflight. Those files are not T0.6 work.
- Worktree `/tmp/obd-t06` on branch `codex/t0.6` was created from `main` at `9c364ca5d5d6637ef01564c98fbc95b572dab6e9`. Its initial status was clean.
- Before T0.6 work, the approved but uncommitted T0.8c follow-up diff from main was applied as baseline to `apps/mobile/App.tsx` and `apps/mobile/metro.config.js` only. These edits belong to T0.8c and must be distinguished from T0.6 changes in review and merge. No T0.5 files were copied.
- Planned T0.6 ownership: `tools/relay/`, the mobile relay mode, `packages/obd-core/src/transport/relay.ts`, and task-scoped workspace scripts/dependencies if sourced in the spec. Exact file list awaits architecture.

## Verification and handoff

- `git worktree add -b codex/t0.6 /tmp/obd-t06 HEAD`: PASS after sandbox escalation for Git metadata write.
- `git apply /tmp/obd-t08c-baseline.patch`: PASS for the two T0.8c mobile baseline files.
- T0.6 product checks: NOT RUN; architecture has not begun. T0.8 phone hardware acceptance: NOT RUN (owner says pending). T0.6 HIL smoke: NOT RUN until owner-arranged phone/car session.
- Remaining review findings: none yet. Blocker: none for local architecture. Next action: architect writes two bounded T0.6 specs, sources allowlist and dependency choices, and identifies any material question before implementation.

## Architecture handoff

- Last tool and role: Codex architect completed; Codex orchestrator handing off T0.6a to implementer. Current stage: T0.6a implementation. Completed stages: preflight, architecture, routine scope confirmation from the user's selection. No material open question.
- Spec paths: `docs/specs/T0.6a-node-relay-mcp.md` and `docs/specs/T0.6b-android-relay-mode.md`, ordered. T0.6a has ten implementation files including `pnpm-lock.yaml`; T0.6b has three. The architect edited specs only; `git diff --check` PASS for them. Product checks NOT RUN.
- Approved decisions: MCP stdio via `@modelcontextprotocol/sdk@1.30.0`; Node `ws@8.21.3`, root `zod@^4.6.5`, `@types/ws@8.18.1`; phone uses built-in WebSocket. The server and phone apply a shared source-backed allowlist; exact Mode 04 needs a fresh on-phone confirmation, and the MCP caller cannot assert approval. `list_signals` explicitly reports no sourced profile signals until T2.1. Raw recordings remain local and gitignored; only the existing redactor produces commit-ready copies.
- Largest risks: broker single-flight/disconnect/recording races in T0.6a and response-reader ownership against the manual console in T0.6b. Hardware verification remains NOT RUN pending the owner-arranged Equinox/phone session. Repair count 0; escalation count 0. Next action: implement T0.6a exactly to its spec, then obtain independent review before T0.6b implementation.

## T0.6a implementation handoff

- Last tool and role: Codex T0.6a implementer completed; Codex orchestrator handing off to independent reviewer. Current stage: T0.6a review. Completed: preflight, architecture, first T0.6a implementation. Touched implementation files: `package.json`, `pnpm-lock.yaml`, `tsconfig.json`, `eslint.config.js`, `packages/obd-core/package.json`, `packages/obd-core/src/transport/relay.ts`, and `tools/relay/{broker.ts,server.ts,server.test.ts,hil-smoke.ts}`. T0.8c `App.tsx`/`metro.config.js` remain baseline, not T0.6a edits.
- Implementer PASS: `pnpm install --frozen-lockfile`; `pnpm exec vitest run tools/relay/server.test.ts` (20 tests); `pnpm exec tsc --noEmit && pnpm exec eslint packages/obd-core/src/transport/relay.ts tools/relay`; `pnpm test`; `pnpm check` (with approved cache access; bridge Ruff/pytest passed); `git diff --check`. Lockfile gained 561 lines for declared MCP/WebSocket dependencies and transitives.
- Implementer NOT RUN: owner-arranged `pnpm hil:smoke`, redaction, and replay; T0.6b app relay is not implemented. Mandatory test evidence for fake-phone WebSocket protocol, MCP registration, recording lifecycle, and async disconnect cases is incomplete despite passing existing tests. Implementer explicitly asks reviewer to gate these items; do not infer compliance from green `pnpm check`.
- Deviations/open findings: test matrix not fully implemented. Repair count 0; escalation count 0. Blocker for T0.6a approval: independent review needed, with likely test/behavior repair. Next action: reviewer inspects all T0.6a changes and reruns spec checks, returns APPROVE or REQUEST_CHANGES; T0.6b waits.

## T0.6a review round 1 and repair handoff

- Last tool and role: Codex independent reviewer returned REQUEST_CHANGES; Codex orchestrator handing off to a fresh T0.6a repair implementer. Current stage: T0.6a repair round 1. Completed stages: preflight, architecture, initial implementation, review round 1. Repair count: 1 of 2 authorized rounds after initial review; escalation count 0 (no tier change warranted by first defects). T0.6b remains untouched and waiting.
- Reviewer PASS: `pnpm install --frozen-lockfile`; `pnpm exec vitest run tools/relay/server.test.ts` (20 but inadequate); `pnpm test`; `pnpm check`; `git diff --check`; declared dependency/status scope; direct allowlist probe (26 accepted, 24 rejected). Reviewer FAIL: real loopback fake phone plus in-memory MCP command probe crashes on first reply; mandatory fake-phone/MCP/recording/protocol matrix absent. Hardware `pnpm hil:smoke`/redaction/replay: NOT RUN pending T0.6b and owner session.
- Blocking findings: `tools/relay/broker.ts:11` Zod discriminated union has two `type: "result"` variants and crashes on a real reply; `tools/relay/server.test.ts:4` lacks the mandated broker/WebSocket/MCP/recording/safety/lifecycle tests.
- Major findings: `broker.ts:87` records promptless partial responses before `server.ts:14` rejects them; `broker.ts:75,87` async message handlers can race and clear pending before recording/result settle; `server.ts:15` fails to split bare-CR ELM lines; `hil-smoke.ts:15` does not compare replay output with the live response.
- Minor finding: `broker.ts:94` rounds timestamps to 10 ms rather than 1 ms/three decimals, and initial meta is not explicitly `t: 0`.
- Next action: fresh implementer fixes all seven findings within T0.6a's owned files and completes the exact mandatory test matrix before reporting; then a new independent reviewer reruns gates. No T0.6b role starts before T0.6a approval.

## T0.6a repair round 1 handoff

- Last tool and role: Codex fresh implementer completed repair round 1; Codex orchestrator handing off to a new independent reviewer. Current stage: T0.6a review round 2. Repair count remains 1 of 2; escalation count 0. Changed during repair: `tools/relay/broker.ts`, `server.ts`, `hil-smoke.ts`, `server.test.ts`; no other stage/file ownership changed.
- Implementer reports all seven findings fixed: Zod result parsing, complete prompt validation before rx recording, serialized broker/recording lifecycle, bare-CR splitting, HIL live/replay byte equality, and exact `t: 0`/three-decimal timestamps. Real loopback fake-phone and in-memory MCP client tests now exercise recording parse/replay, busy, Mode 04 denial, disconnect, and replacement phone.
- Implementer PASS: `pnpm install --frozen-lockfile`; `pnpm exec vitest run tools/relay/server.test.ts` (6 integrated cases); `pnpm check`; `git diff --check`. The loopback bind required approved elevated execution after default sandbox `listen EPERM`. Hardware `pnpm hil:smoke`/redaction/replay NOT RUN (requires T0.6b and owner-arranged session).
- Remaining findings pending independent re-review: all seven round-1 findings must be rechecked; implementer claims closure but no reviewer approval yet. Next action: new reviewer independently reruns all T0.6a local verification, including real broker/MCP behavior, and returns APPROVE or REQUEST_CHANGES.

## T0.6a review round 2 and final repair handoff

- Last tool and role: Codex independent reviewer returned REQUEST_CHANGES; Codex orchestrator handing off to a fresh escalated implementer for repair round 2. Current stage: T0.6a repair round 2 (final allowed repair). Completed: preflight, architecture, initial implementation, review rounds 1–2, repair round 1. Repair count: 2 of 2; no further repair round is authorized if the next review still requests changes.
- Reviewer PASS: `pnpm install --frozen-lockfile`; `pnpm exec vitest run tools/relay/server.test.ts` (6 cases, insufficient); `pnpm test`; `pnpm check` (TypeScript, lint, core/mobile/relay tests, Ruff, pytest); `git diff --check`; task-only scope; four declared dependency versions/lockfile transitives. Reviewer independently reproduced a real loopback result-then-close race: valid `4100\r>` response rejected as disconnected. Hardware remains NOT RUN.
- Blocking findings: `tools/relay/broker.ts:81-85,103-111` handles socket close/error outside the message queue, so a complete success can lose to disconnect while rx append is awaited; `tools/relay/server.test.ts:43-103` lacks mandatory Mode 04 approval/denial, pre-dispatch, pending disconnect/replacement, MCP schema, recording path/digest, token/protocol/oversize/binary/pre-hello/malformed/wrong-id/duplicate/invalid-base64, and race regression cases.
- Major findings: `broker.ts:67,118-120` uses `Date.now()` so timestamps can move backward; `hil-smoke.ts:17-26` validates but does not print the required prompt-terminated `4100` response and replay-match result. Prior duplicate Zod discriminator, promptless rx, CR splitting, live/replay comparison, t:0, and precision findings are closed by reviewer.
- Escalation: 1 of 1 for the T0.6a implementer stage, from `gpt-5.6-terra`/medium to `gpt-6-sol`/high. Concrete reason: the same asynchronous settlement defect survived repair round 1 and was reproduced by a second reviewer; the mandatory integrated test matrix was again incomplete. This escalation applies only to the remaining T0.6a repair, not architecture, review, or T0.6b.
- Next action: fresh escalated implementer fixes the race, completes every mandatory local test row, monotonic timing, and HIL evidence output within T0.6a ownership; then a fresh independent reviewer reruns the full gate. Stop without T0.6b if the next verdict still requests changes.

## T0.6a final repair handoff

- Last tool and role: Codex escalated implementer completed repair round 2; Codex orchestrator handing off to a fresh independent reviewer. Current stage: T0.6a review round 3, final gate. Repair count 2 of 2; implementer escalation count 1 of 1 for this stage.
- Repair touched only `tools/relay/broker.ts`, `server.test.ts`, and `hil-smoke.ts`. Implementer reports socket messages/errors/closes now settle in queue order, timestamps use monotonic time, HIL prints response and replay result, and the real loopback/MCP suite covers result-then-close plus the remaining policy/protocol/recording cases.
- Implementer PASS: `pnpm install --frozen-lockfile`; focused relay suite (27 passed); `pnpm test`; `pnpm check` (TypeScript, lint, tests, bridge Ruff and pytest); `git diff --check`; status/lockfile scope. Loopback bind required approved sandbox escalation. HIL smoke/redaction/replay NOT RUN pending T0.6b and owner session.
- Remaining findings: round-2 blockers and majors are claimed fixed but must be independently verified. Next action: fresh reviewer reruns all spec-required local gates and returns APPROVE or REQUEST_CHANGES. If REQUEST_CHANGES, stop T0.6a without a third repair round and do not start T0.6b.

## T0.6a final review and stop

- Last tool and role: Codex independent reviewer returned REQUEST_CHANGES in review round 3; Codex orchestrator recorded the handoff and stopped T0.6a. Current stage: STOPPED at the `$obd-feature` two-repair limit. Completed: preflight, two ordered specs, initial T0.6a implementation, two bounded repair rounds, three independent reviews. T0.6b implementation and review are NOT RUN. Repair count 2 of 2; T0.6a implementer tier escalation count 1 of 1 (reason above). No further repair or tier retry is authorized in this run.
- Reviewer PASS: `pnpm install --frozen-lockfile`; `pnpm exec vitest run tools/relay/server.test.ts` (27/27); `pnpm test` (core 83, mobile 50, relay 27); `pnpm check` (TypeScript, lint, tests, Ruff, pytest); `git diff --check`; real result-then-close regression; policy, Mode 04, single-flight, MCP tool list/schema, recording/order/path/digest/replay, monotonic timing, scope/dependency/purity/provenance audits. Hardware HIL smoke/redaction/replay NOT RUN pending T0.6b, T0.8 phone acceptance, and owner session.
- Remaining **blocking** finding: `tools/relay/broker.ts:139` accepts malformed base64 with extra padding; independent real loopback probe sent invalid `NDEwMA0+=` and the broker fulfilled the command. Existing tests cover only `%%%`. This violates T0.6a's strict invalid-base64 requirement.
- Remaining **major** finding: `tools/relay/broker.ts:34` ignores `OBD_RELAY_TOKEN`; the reviewer set `OBD_RELAY_TOKEN=review-fixed` and observed `broker.token !== "review-fixed"`. This violates the configured-token contract.
- Branch/worktree: `codex/t0.6` at `/tmp/obd-t06`, uncommitted. It contains the two T0.6 specs, partial T0.6a implementation and this record, plus the imported pre-existing T0.8c `App.tsx`/`metro.config.js` baseline. No T0.5 work was copied, no T0.6b product files were changed, and nothing was merged or committed.
- Blocker and next action: T0.6a cannot be approved or advanced to T0.6b under the current `$obd-feature` run. A new user decision or a later authorized continuation is required to handle the two exact findings and arrange another independent gate; preserve the branch and this record for that handoff. Do not claim T0.6 completion or tick the plan.

## Claude takeover note (2026-09-23)

- Owner reported Codex stopped T0.6a at the repair cap with review REQUEST_CHANGES: `tools/relay/broker.ts:139` accepts malformed base64 with extra padding; `tools/relay/broker.ts:34` ignores `OBD_RELAY_TOKEN` (the relay would accept unauthenticated clients on the LAN). T0.6b not started; nothing committed.
- Worktree moved by Claude from `/tmp/obd-t06` to `/home/kema/projects/obd-t06` (`git worktree move`) so a WSL restart cannot delete the uncommitted work. Paths in this record that say `/tmp/obd-t06` now refer to `/home/kema/projects/obd-t06`.
- Owner authorized Claude to complete the work ("go ahead as you see fit"), which covers one escalation round past the repair cap. Plan: after the main-branch write-safety task (`docs/specs/X-2026-09-23-write-safety.md`, new `packages/obd-core/src/elm/guard.ts` allowlist) is approved and committed, bring this branch up to `main`, make the relay enforce that shared allowlist, fix both findings, and run a fresh independent review. Escalation count: 1 (owner-authorized).

## Codex handoff: two security findings (2026-09-24)

- Last tool: Codex; role: implementer; current stage: owner-authorized bounded repair of two open T0.6a security findings. Claude's T0.6 role has stopped for this handoff; Claude reviewer will independently check these fixes afterward.
- Approved scope: only `tools/relay/broker.ts:34` honoring `OBD_RELAY_TOKEN` and `tools/relay/broker.ts:139` rejecting non-canonical base64, with failure modes added to T0.6a Verification before tests, then tests written before code in `tools/relay/server.test.ts`. No rebase, guard.ts integration, T0.6b, hardware, commit, or main-worktree edit.
- Worktree/branch: `/home/kema/projects/obd-t06`, `codex/t0.6` at `9c364ca5d5d6637ef01564c98fbc95b572dab6e9`; pre-existing T0.6a implementation/specs/record are uncommitted, and T0.8c `apps/mobile/App.tsx`/`metro.config.js` changes remain baseline. Baseline `git status --short --untracked-files=all` at handoff contains only those previously documented files; no new Codex edit yet.
- Historical repair count remains 2 of 2. Owner-authorized escalation beyond that cap is recorded in the Claude takeover note as count 1; do not reset it. Verification for this handoff: NOT RUN yet. Remaining findings: configured token ignored; malformed base64 padding accepted. Next action: record failure modes in spec, add one failing test per mode, fix only broker behavior, run focused relay tests, `pnpm check`, and `git diff --check`, then report to Claude reviewer.

## Codex security repair report and Claude review handoff (2026-09-24)

- Last tool: Codex; role: implementer completed the owner-authorized bounded repair. Current stage: awaiting fresh independent Claude reviewer. Completed in this handoff: failure modes documented first in `docs/specs/T0.6a-node-relay-mcp.md` Verification, one integration test per finding added to `tools/relay/server.test.ts`, both tests observed FAIL against unchanged `tools/relay/broker.ts`, then broker fixed and checks rerun. Task-touched files are exactly those three; no rebase, guard.ts integration, T0.6b, app, obd-core, or main-worktree edit. Historical repair count 2 of 2 and owner-authorized escalation count 1 are preserved, not reset.
- Red-test command, using approved loopback execution: `pnpm exec vitest run tools/relay/server.test.ts --testNamePattern='uses the exact configured environment token|rejects noncanonical base64 padding'` — FAIL as expected before broker edits. Token assertion: expected `configured-token`, received `injected-token`. Base64 assertion: pending result resolved with bytes `[52,49,48,48,13,62]`, `ms: 1`, for malformed `NDEwMA0+=` instead of rejecting. These are failure evidence, not a final check failure.
- Fix 1: broker token selection is now `process.env.OBD_RELAY_TOKEN ?? options.token ?? randomBytes(32).toString("base64url")`. With the variable set, only that exact value passes the existing pre-upgrade equality check; when unset, injected test token or generated random token remains. Integration test rejects the injected value and accepts the configured value over the WebSocket path.
- Fix 2: base64 decode now re-encodes decoded bytes and requires exact equality with the input. This rejects extra padding or stray characters such as `NDEwMA0+=` before the result is accepted. Integration test asserts protocol rejection, pending failure, socket close, and recording directions `[meta, tx, meta]` with no successful rx.
- Verification PASS: `pnpm exec vitest run tools/relay/server.test.ts` (29/29); `pnpm check` (TypeScript, lint, all TypeScript/mobile/relay tests, bridge Ruff and pytest); `git diff --check`. First `pnpm check` reached bridge and FAILed because a stale ignored `.venv/bin/pytest` shebang still pointed to old `/tmp/obd-t06/...`. `uv sync --frozen --all-groups --reinstall` repaired that local tool state without tracked file changes; rerun `pnpm check` PASS. Hardware `pnpm hil:smoke`, redaction, replay: NOT RUN (owner session and T0.6b absent). No commit.
- Remaining review findings: the prior two findings are claimed fixed but are not independently approved. Blocker for T0.6a approval: Claude reviewer must rerun the two tests and full required gate. Next actions after that review: once write-safety, T0.8c, and T0.8d are committed on main, rebase `codex/t0.6`, switch the relay to reviewed `packages/obd-core/src/elm/guard.ts`, and obtain fresh review. Until then do not rebase, integrate guard.ts, start T0.6b, or run hardware.

## Claude orchestrator: branch brought up to main (2026-09-24)

- Owner request "do both in parallel" (T0.5 Stage C on main, T0.6 here). The Codex security repair (token, base64) is still awaiting its independent review. That review now runs together with the guard integration below, as one final gate.
- The T0.8c `apps/mobile` edits in this worktree were dropped: they are committed on main as part of `056bce9`. The T0.6a work was stashed, `codex/t0.6` was fast-forwarded `9c364ca → 056bce9` (main: write-safety `guard.ts`, T0.5 A+B, T0.8 done), and the stash was restored with no conflicts. Backups: `t06-tracked.patch` and `t06-untracked.tgz` in the orchestrator scratchpad.
- Next: the architect amends `docs/specs/T0.6a-node-relay-mcp.md` so the relay enforces `packages/obd-core/src/elm/guard.ts`, instead of the separate allowlist in `src/transport/relay.ts`. The amendment also covers:
  - the write-safety round-3 follow-up: a fresh session's first write, which is the relay reconnect case;
  - the owner-accepted Mode 06 gap.
  Then the implementer, then a fresh reviewer. Escalation count stays 1 (owner-authorized). This is its single round.
- 2026-09-24 Claude architect: wrote "Amendment 2026-09-24: shared write guard" at the end of `docs/specs/T0.6a-node-relay-mcp.md` (relay delegates to `guard.ts`; broker tracks header state and an AT-first stale rule per connection; Mode 06 refused; `hil:smoke` starts from the recorded phone init; failure modes F1–F11 mapped to tests). No code changed; no open owner question.

## Claude implementer: amendment 2026-09-24 shared write guard (2026-09-24)

- Last tool and role: Claude implementer completed the amendment; next is a fresh independent reviewer (single owner-authorized round; escalation count stays 1). Touched: `packages/obd-core/src/transport/relay.ts`, `tools/relay/broker.ts`, `tools/relay/server.test.ts`, `tools/relay/hil-smoke.ts`, one bullet at the end of `docs/ELM327.md` §Write safety, Input rules (the amendment's sentence verbatim), and this record. `guard.ts`, `session.ts`, `server.ts`, manifests and lockfile unchanged.
- Tests first: T1 (17 refused rows + `ATAT1` allowed), T2, T3, T4, T5, T6, T7 (timeout, transport-error, STOPPED) and the `two-prompts` row added; `prime()`/`exchange()`/`refused()` helpers; `fixture()` primes with `ATI` and captures logger lines; the MCP replacement-phone test primes the replacement. Removed the two policy-table tests and the `accepted`/`rejected` constants as the amendment names. Red run against unchanged `relay.ts`/`broker.ts`: 27 failed / 27 passed of 54 (every T1 row, T1 `ATAT1`, T2–T7 rows, `two-prompts`); causes were wrong refusal messages/no log line, commands reaching the fake phone (5 s timeouts on `0100FF`, `014FFFFFF1`, `01`, `0600`, T2–T7), `ATAT1` refused, and `two-prompts` resolving instead of rejecting.
- Green: `pnpm exec vitest run tools/relay/server.test.ts` 54/54 (15 consecutive runs), both Codex security tests unchanged apart from priming via `fixture()`. `pnpm check` PASS three consecutive runs (obd-core 131, mobile 57, relay 54, Ruff, pytest); obd-core guard/session/replay tests untouched. `git diff --check` PASS; untracked task files have no trailing whitespace. Purity grep on `relay.ts` empty. No `.venv` repair or reinstall was needed.
- One flake observed: the first `pnpm check` run failed once in "rejects noncanonical base64 padding …": recording read `[meta, tx]` without the final meta. Cause is pre-existing and not in amendment scope: `disconnect()` clears `phone` before `endRecording()` appends the meta line, and that test reads the file as soon as `isConnected()` is false. The amendment allows that test to change only by priming, so it was not touched. Reviewer should decide whether it needs a follow-up (for example `waitForLines` in the test).
- Mutation checks, on a copy in the session scratchpad (deleted afterwards), each killed its named test: M1 old `authorizeRelayCommand` → all T1 rows + T2; M2 no `04` branch → Mode 04 MCP test (+ T2, denial test); M3 no `06` refusal → T1 `0600`; M4 no refusal log → all 17 T1 refused rows; M5 `request()` trusts caller's object → T2; M6 skip `beforeWrite` → T3 (+ T4, T5); M7 `afterReply(…, "ok")` → T4; M8 no header reset on hello → T5; M9 `idle = true` on hello → T6; M10 no idle clear on `started` → T7 timeout (+ transport-error); M11 `idle = true` on any success → T7 STOPPED; M12 no single-`>` check → `two-prompts`.
- Deviation: T7 uses an unprimed phone as specified but sends `ATI` itself before the failing exchange, because without an idle state first the timeout row cannot detect mutation 10.
- Hardware `pnpm hil:smoke` (now `ATZ ATE0 ATL0 ATS0 ATH1 ATSP0 0100` with per-command replay match), redaction, and replay: NOT RUN (T0.6b and the owner session are absent). No commit.

## Claude final review, escalation round (2026-09-24): APPROVE with two majors

- `pnpm check` PASS in 5 consecutive runs (obd-core 131, mobile 57, relay 54). 27/27 local items satisfied. Both Codex security fixes were probed over loopback. The spec's 12 mutants are all killed. The amendment decisions all match the code. All three implementer deviations are accepted. The hil:smoke success path was checked against a fake phone replaying the phone-console replies.
- 1. [major] `broker.ts:41`: an empty `OBD_RELAY_TOKEN=""` is used as the token, so `/phone?token=` connects. A blank `.env` line turns off authentication on 0.0.0.0. Fix: treat empty as unset, or refuse to start.
- 2. [major] `hil-smoke.ts:21` + `broker.ts:141`: on a mid-sequence disconnect, `finally` → `stopRecording()` throws "no recording active", which hides "relay: disconnected". `broker.close()` is skipped and the final disconnect meta is lost. No false PASS.
- 3. [minor] `broker.ts:141`: `disconnect()` fails the pending call before `endRecording()` appends the meta. MCP callers cannot observe this, but it makes the base64 test flaky, and it causes finding 2. Fix: await `endRecording()` before `failPending()`.
- 4. [minor] `server.test.ts:178-179`: a fixed `delay(5)` before asserting disconnect is flaky (1 failure in 30 runs). Use `waitFor`.
- 5. [minor] `broker.ts:127`: no test pins "a failed result never calls afterReply". Add a row: ATZ, ATSP7 timeout, `ATSH DA1DF1` refused.
- 6. [minor, decision] The AT-first rule assumes only one dropped character, which is what the datasheet documents. If two were dropped, `ATE0` would become `E0`, which is valid hex. The safer option is to admit only `ATI`/`ATZ` while stale.
- Escalation count 1 is used up. The fix round for findings 1–6 needs the owner's decision.

## Claude implementer: owner-approved fix round for findings 1–6 (2026-09-24)

- Last tool and role: Claude implementer. Next: independent review of this round. Touched: `tools/relay/broker.ts`, `tools/relay/hil-smoke.ts`, `tools/relay/server.test.ts`, `docs/specs/T0.6a-node-relay-mcp.md` (appended "Amendment 2 (2026-09-24, owner-approved fix round)" as the owner directed), the relay sentence in `docs/ELM327.md` §Write safety, Input rules (it said "only AT commands"), and this record. `relay.ts`, `guard.ts`, `session.ts`, `server.ts`, manifests and lockfile unchanged.
- Fixes: (1) an empty `OBD_RELAY_TOKEN` is unset. (2+3) `disconnect()` does `try { await endRecording() } finally { failPending() }`. In `hil-smoke`, `startRecording` runs inside `try`. A sequence error calls `stopRecording().catch(() => undefined)` and rethrows the original error. `broker.close()` runs in `finally`. (4) `delay(5)` at the old lines 178–179 became `waitFor` (bad socket CLOSED, then `!isConnected()`). (5) New T8: `ATZ` → `ATSP7` phone `timeout` → `ATI` → `ATSH DA1DF1` refused with "header/protocol sequence". (6) While stale, only `ATI`/`ATZ` get through. Message: `relay: refused, ELM state unknown; send ATI or ATZ first`.
- hil-smoke testability: `main` is exported and takes an optional broker (default `new RelayBroker()`). It resolves the recording path against `broker.root`, which equals cwd by default, so the CLI behaves the same. A subprocess test was rejected: `OBD_RELAY_PORT` rejects 0, so it would need a fixed port.
- Test changes: `refused()` gained an optional reason. T4 now sends `ATI` after the `?` reply, because under fix 6 the stale rule would otherwise refuse `ATSH` and hide mutation 7. T6 also refuses `ATE0`. T6 and T7 titles now say "ATI or ATZ".
- Red run (new tests written first, on the pre-fix code): 4 failed / 54 passed of 58:
  - empty token: `expected '' not to be ''`
  - disconnect-order: `[meta, tx]` vs `[meta, tx, meta]`
  - hil:smoke: got `relay: no recording active` instead of `relay: disconnected`
  - T6: `ATE0` reached the phone (5 s timeout)
  T8 and the updated T4 passed on the pre-fix code, as expected: they pin existing correct behaviour. They were proven by mutation instead.
- Green: `pnpm exec vitest run tools/relay/server.test.ts` 58/58 in 22 runs (12 before a one-line lint fix, 10 after), no flakes. `pnpm check` PASS in 3 consecutive runs after the lint fix: obd-core 131, mobile 57, relay 58, Ruff, pytest. The first 2 attempts FAILED on lint only (`no-unnecessary-condition` on a `file === undefined` guard in the new hil test; removed). `git diff --check` PASS; untracked task files have no trailing whitespace.
- Mutation checks on a copy in the session scratchpad (deleted afterwards); each killed exactly its named test:
  - M1 env `""` used as the token → empty-token test
  - M2 old `finally { stopRecording() }` → hil:smoke test
  - M2b `close()` moved out of `finally` → hil:smoke test
  - M3 `failPending` before `endRecording` → disconnect-order test (5/5 runs, deterministic because it reads with `readFileSync` right after the rejection)
  - M5 a failed result calls `afterReply(…, "ok")` → T8
  - M6 stale rule back to `startsWith("AT")` → T6
  - M7 `afterReply` always `"ok"` → T4, which still detects it after the `ATI` change
  Fix 4 is a flake fix with no mutant.
- Hardware `pnpm hil:smoke`, redaction, replay: NOT RUN (T0.6b and the owner session are absent). No commit.

## Claude re-review of the fix round (2026-09-24): APPROVE

- `pnpm check` PASS 3/3 (relay 58); the relay suite passed 10/10 with no flakes. All 6 fixes are closed:
  - The empty-token case was probed over loopback.
  - Disconnect ordering was checked: no deadlock, and the `endRecording` throw path was probed.
  - hil-smoke closes the broker on every path.
  - All 8 mutants were each killed by their named test. All 4 deviations are accepted.
- Open minors (T0.6b follow-ups):
  1. `hil-smoke.ts:25`: the final `stopRecording()` sits outside the catch, so a disconnect right after the last `0100` reply can give a false FAIL ("no recording active"). There is no false PASS, and it did not reproduce in 40 loopback runs.
  2. `docs/ELM327.md:133`, `broker.ts:84`: the wording says the datasheet "does not bound" the number of dropped characters. Restate it as the documented one-character model, with ATI/ATZ as extra margin for a second dropped character; if all three characters were lost, a bare CR would repeat the previous command.
  3. (note) A whitespace-only `OBD_RELAY_TOKEN` is still used as the token.
- T0.6a is CLOSED (hardware NOT RUN, which needs T0.6b). Next: commit on `codex/t0.6`, merge into main, then T0.6b.
