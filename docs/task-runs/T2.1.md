# T2.1 task run

## Current state (as of 2026-09-25)

- **Stage:** CLOSED. T2.1b (29-bit profile scan) and T2.1a (OBDb import/decode) each independently **APPROVE** with zero findings; repair 0/2, escalation 0. Committed as `04d0f0a` (`t2.1 done`).
- **Evidence:** profile CLI SHA-256 `7f75f3e3…` on both redacted spikes (run twice, identical); replay artifact hashes `1f4573cc…` / `3ac05e2e…` / `3f2f94c0…` (spike, spike-2, phone-console); both spikes yield five `community` DACB readings, `33E5` stays `nodata`. OBDb vendor bytes match pinned commit `15ee122` with LICENSE attribution.
- **NOT RUN:** new hardware session (the two committed redacted recordings satisfied the replay evidence). `pnpm replay` wrapper FAILs only on the sandbox `tsx` IPC `EPERM`; the spec's direct-Node fallback PASSes — an environment limitation, not a product defect.
- **Next action:** none for T2.1; tier promotion is T2.2's job.

## Preflight and ownership

- Last tool and role: Codex orchestrator preflight (2026-09-24). Current stage: architecture. Completed stages: preflight. Other tool or role on T2.1: none reported. Repair count 0; escalation count 0/none.
- User-approved scope: PLAN T2.1 vehicle profiles, sourced Equinox Mode 22 signals, OBDb importer and provenance tiers. Split into bounded specs if needed. No commits or PLAN tick. Do not touch the separate `/home/kema/projects/obd-t06` worktree, T0.6 relay/transport work, or T0.7 guard work. Read only committed `*.redacted.jsonl` Equinox recordings; never read raw recordings.
- Baseline: main HEAD `eda723f`; initial `git status --short` empty. T0.6 is in a separate worktree. Existing core vehicle profile is only generic. OBDb Equinox signalset is not yet vendored. Sources to evaluate: PLAN T2.1, ADR-010 and ADR-012, ARCHITECTURE, ELM327, committed redacted Equinox recordings, and the upstream OBDb repository with pinned revision and license.
- Verification: preflight status and source inspection PASS. Architecture, implementation, tests, hardware, and independent review NOT RUN. Hardware checks require a real recording and cannot be inferred from fixtures. Next action: architect writes bounded spec(s) and states any evidence or boundary limits.

## Architecture handoff

- Last tool and role: Codex architect completed; Codex orchestrator handing off T2.1b to a fresh implementer. Current stage: T2.1b implementation. Completed stages: preflight, architecture. Specs: `docs/specs/T2.1b-equinox-profile-scan.md` and `docs/specs/T2.1a-obdb-equinox-import.md`. Repair count 0; escalation count 0/none.
- Approved order: T2.1b adds the 29-bit profile scan from two committed redacted recording tails; T2.1a then vendors, imports, and decodes the pinned OBDb signalset. Neither changes the guard, relay, mobile app, or recordings. The architect identified no material decision requiring further owner input.
- Source acquisition: official OBDb clone fetched into `/tmp/t21-obdb-equinox`; pinned commit `15ee122df435d541d928dac8e7532bf43e39bdd4`. Signalset and LICENSE SHA-256 values appear in T2.1a. This was a read-only source fetch, not a dependency or repo change.
- T2.1b permitted implementation files: `packages/obd-core/src/vehicles/{profile,scan}.ts`, `packages/obd-core/scripts/profile-scan-replay.ts`, `packages/obd-core/test/profile-scan.test.ts`, `fixtures/synthetic/profile-setup-failure.jsonl`. Verification and review NOT RUN. Next action: implement T2.1b tests and synthetic fixture first, confirm baseline failure, then product code and required gates.

## T2.1b implementation handoff

- Last tool and role: fresh Codex implementer (`gpt-6-sol`/medium per standing owner override) completed; Codex orchestrator handing off to fresh independent reviewer. Current stage: T2.1b review. Completed stages: preflight, architecture, T2.1b implementation. Repair count 0; escalation count 0/none.
- Implementer changed only the five T2.1b implementation files in its spec and reports writing tests first. Baseline focused run failed because `scan.js` did not yet exist. Focused test PASS: 11 tests. `pnpm check` PASS (176 core tests, 60 mobile tests, Python Ruff/pytest). `git diff --check` PASS.
- Profile CLI PASS on both committed redacted spike recordings; each run repeated and compared identical. Output SHA-256 for both is `7f75f3e3042236152c220a1cd3faa6507f75eb3f250904e6a467a4a488d49a96`. Whole-recording replay PASS for both via `node --import tsx packages/obd-core/scripts/replay.ts <recording>`. `pnpm replay` FAIL to launch in sandbox due `tsx` CLI IPC `EPERM` at `/tmp/tsx-1000/14.pipe`; the same Node script executed successfully via the spec's fallback. Hardware NOT RUN; no owner-arranged session.
- Remaining findings: independent review pending. Next action: fresh reviewer inspects all five files and independently regenerates the focused tests, profile artifacts, full replays, `pnpm check`, and diff hygiene, then returns APPROVE or REQUEST_CHANGES.

## T2.1b independent review and T2.1a handoff

- Last tool and role: fresh Codex independent reviewer **APPROVE** for bounded T2.1b; Codex orchestrator handing off T2.1a to a fresh implementer. Current stage: T2.1a implementation. Completed stages: T2.1b architecture, implementation, and independent review; T2.1a architecture. Repair count 0; escalation count 0/none. Full PLAN T2.1 remains open until T2.1a review.
- Reviewer independently reproduced PASS: 11 focused tests, both profile artifacts twice with the recorded SHA-256, direct Node replay of both full redacted recordings, `pnpm check` (176 core and 60 mobile tests plus Python checks), and `git diff --check` including untracked files. `pnpm replay` wrapper FAIL due sandbox `tsx` IPC `EPERM`; direct Node replay PASS per spec fallback. No findings. Hardware NOT RUN; no owner-arranged session.
- T2.1a permitted implementation files: vendored `packages/obd-core/vehicles/chevrolet-equinox-ev/{default.json,LICENSE,README.md}`, `packages/obd-core/src/vehicles/obdb/{import,decode}.ts`, `packages/obd-core/scripts/replay.ts`, and `packages/obd-core/test/obdb-equinox.test.ts`. Exact upstream source is pinned in `docs/specs/T2.1a-obdb-equinox-import.md` and available in `/tmp/t21-obdb-equinox`. Preserve approved T2.1b files. Next action: fresh implementer writes T2.1a tests before product code, proves baseline red, copies exact upstream data/license, implements importer/decoder and replay integration, and runs required gates.

## T2.1a implementation handoff

- Last tool and role: fresh Codex implementer (`gpt-6-sol`/medium per standing owner override) completed; Codex orchestrator handing off to fresh independent reviewer. Current stage: T2.1a review. Completed stages: T2.1b review, T2.1a implementation. Repair count 0; escalation count 0/none.
- Implementer changed only seven T2.1a files in its spec. The upstream signalset and license hashes match the pinned spec. Tests were written first; baseline failed because `decode.js` did not exist. Focused test PASS: 9 tests. `pnpm check` PASS (185 core tests, 60 mobile tests, Python checks). `git diff --check` PASS.
- Direct Node CLI replay artifacts regenerated twice and compared: first spike SHA-256 `1f4573cccee2e9bd963243307e4fcccd0367e8953f7a2e79378c35a73a7d30d6`; second spike `3ac05e2e44b2b7743ea12a0726f110797ba04448c6725ef80716ccb03e8611bd`; phone console `3f2f94c017202e58563b8cbfdc98cd310dd7514413bbcc267f42c7ab3d7a5d6e`. Both spike outputs show five `community` readings and no `33E5` reading; phone console has no new reading. `pnpm replay` FAIL to launch due sandbox `tsx` IPC `EPERM`; spec fallback direct Node CLI PASS. Hardware NOT RUN, no owner-arranged session.
- Remaining findings: independent review pending. Next action: fresh reviewer checks all seven files and exact vendor bytes, regenerates replay artifacts and hashes, focused tests, `pnpm check`, and diff hygiene; gives APPROVE or REQUEST_CHANGES.

## T2.1a independent review and close

- Last tool and role: fresh Codex independent reviewer **APPROVE** for T2.1a; Codex orchestrator closed the bounded T2.1 software workflow. Current stage: software review closed. Completed stages: architecture, T2.1b implementation/review, T2.1a implementation/review. Repair count 0 of 2; escalation count 0/none. No blocking, major, or minor findings. No commit or PLAN tick.
- Reviewer PASS: T2.1a focused suite (9), T2.1b profile suite (11), `pnpm check` twice (185 core tests, 60 mobile tests, TypeScript, lint, Ruff, Python pytest), and `git diff --check`. The pinned upstream JSON and LICENSE hashes match exact copied bytes and attribution is present. Core remains platform-independent; no recording, dependency, guard, relay, app, or secret change was made.
- Reviewer regenerated each direct Node replay output twice and matched all three hashes recorded above. Both spikes yielded the five OBDb `community` readings, `33E5` remained `nodata`, and phone-console replay had no additional reading. The T2.1b profile scan artifacts and full-recording fallback replays were independently verified in its earlier review. `pnpm replay` wrapper FAIL to launch in sandbox due `tsx` IPC `EPERM`; direct `node --import tsx` execution of the same script PASS. This is an environment execution limitation, not a data or decoder failure.
- New hardware session NOT RUN: no owner-arranged vehicle session. The two committed redacted Equinox recordings satisfy the required T2.1 replay evidence. T2.2 still needs dash/plausibility comparison before any signal may be promoted from `community` to `verified`; no battery-health claim follows from this task. Next action: owner reviews uncommitted diff and, when ready, arranges a T2.2 validation session or directs the next scoped task.
