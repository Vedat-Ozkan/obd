# T0.7 task run

## Ownership

- Last tool: Claude
- Last role: reviewer (B2 re-review: APPROVE, 2026-09-24)
- Current stage: A2 (waits for the T0.6 merge); A1, B1, B2 CLOSED 2026-09-24
- Completed stages: preflight, architecture, decisions (owner Q1–Q2, orchestrator Q3–Q7), A1 implementation, A1 review
- Other active tool or role: Codex T2.8 in `apps/mobile` (App.tsx, `src/garage/`); Claude T0.6a re-review in the `obd-t06` worktree. T0.5 Stage C and T0.10b are committed (`6664444`).

## Contract

- Task: PLAN T0.7. Part 1 is the three owner-accepted write-safety follow-ups (`docs/task-runs/X-2026-09-23-write-safety.md` §Follow-ups):
  - Mode 06 length source, or refuse `06`;
  - the §Write safety residual sentence;
  - the discovery replay runtime note.
  Part 2 is the `obd-core` codes report model and the "recently cleared" heuristic: no stored DTCs AND monitors incomplete AND low PID 30/31/4E, with Mode 0A permanent DTCs as a strong signal. The report is a typed object plus a template renderer. Every battery report embeds it.
- Also carried in (T0.5 Stage C post-implementation note): when freeze-frame PID 02 is `00 00`, the report shows no freeze-frame data.
- Spec paths: `docs/specs/T0.7-codes-report.md` (stages A1 → B1 → B2, then A2; A2's `docs/ELM327.md` §Write safety edit waits for the T0.6 merge and the T0.5 Stage C commit)
- Approved decisions: owner "what else can we work on right now" (2026-09-24), under the standing delegation "go ahead as you see fit". T0.7 is next in the Phase 0 order.
- Repair count: 1 of 2 (B2)
- Escalation count: 0

## Baseline

- `main` at `056bce9`. Uncommitted and approved: T0.5 Stage C (`freeze.ts`, `replay.ts`, `ELM327.md` §J1979 rows) and Codex T0.10b (observation labels). Both are waiting for the owner's commit.

## A1 implementation

2026-09-24, Claude implementer. Files: `packages/obd-core/src/elm/guard.ts`, `packages/obd-core/test/guard.test.ts`, `apps/mobile/src/console.ts`, `apps/mobile/test/console-recording.test.ts`, `docs/discovery-2026-09.md`. `docs/ELM327.md` untouched (A2).

- Baseline before any edit: `pnpm replay` of the three tracked redacted recordings and the three synthetic fixtures saved to the scratchpad (all exit 0).
- Tests first: G2 allowed set without `06`; G6 adds `2026-09-24-phone-console.redacted.jsonl`; G8 lengths without `06`, refuses `06`, `0600`, `06 00`, `020201`, `0202FF`, `02 02 01`, allows `020200`, `02 46 00`. Console: list assertion without `06`, `0600` in `rejected`. On the old source: guard G2 and G8 FAIL; console "rejects \"0600\"", the list assertion, and "rejects before any transport write" FAIL.
- Code: `READ_LENGTHS` drops `06`; a `02` request must end in `00` (comment cites §J1979 conventions "Mode 02 reply layout", phone-console L38/L42, Decisions 4–5). `READ_ONLY_SERVICES` drops `06`. Discovery §1 Replay row gains the dated 2026-09-24 runtime sentence.
- After: guard 13/13, console 41/41. Six tracked/synthetic replay outputs byte-identical to baseline (`diff -r` empty). Local gitignored `2026-09-22-spike.jsonl`, `2026-09-22-spike-2.jsonl`, `2026-09-24-phone-console.jsonl`, `2026-09-23-discovery-targeted.jsonl`: exit codes, stdout line and byte counts, and stderr line counts identical before and after (contents not printed). Full `2026-09-23-discovery.jsonl` replay not run (~1.5 h).
- Mutation (scratch copy outside the repo): `"06": 4` restored → G2 and G8 FAIL; frame check removed → G8 FAIL (`020201`).
- `pnpm check` exit 0 (obd-core 144/144, mobile 60/60). `git diff --check` clean. Hardware: none required; NOT RUN.

### A1 round 1 (Claude reviewer, 2026-09-24): APPROVE

- 6/6 A1 items. `pnpm check` PASS (obd-core 144, mobile 60). Replay output is byte-identical to a HEAD build. No tx line in any tracked or local recording is refused by the new guard (all tx parsed, counts only). All mutations caught. Sources checked.
- 1. [minor, folded into A2] `guard.ts:24-27`: the single-use `allowedRequest` helper can be inlined.
- 2. [minor, folded into A2] `guard.ts:12-13`: the comment should say the leftover byte can still be a refused service (e.g. `14`), per the DS p.32 reading. A2 adds `guard.ts` (comment only) to its file list so the comment and the doc stay in step.
- 3. [minor, CLOSED by orchestrator] stale handoff header.

## B1 implementation

2026-09-24, Claude implementer. Files: `fixtures/synthetic/codes-{cleared,permanent,stored,conflict}.jsonl` and their `.replay-label.json` companions, `packages/obd-core/test/observation-labels.test.ts`, `fixtures/README.md`. No report code.

- Test first: `observation-labels.test.ts` gained the four pairs and "exactly the ten companion labels"; it failed (5 of 11) before the fixtures existed.
- Fixtures: send-only, no init, meta `synthetic: true`, `car`/`dongle` `none`, note per spec (the `§` written as the JSON escape `\u00a7` so the file stays ASCII). Layouts: §J1979 conventions (bitmaps, PID 01 layout and polarity, DTC lists and 2-byte encoding, Mode 02 reply layout, freeze PID 02); PID byte counts and scaling from `MODE01_PIDS` (vendored OBDb). DTC values reuse the §J1979 / T0.5 examples (`01 33` P0133, `C1 58` U0158). Policy values (Decisions 1): cleared `0130`=2, `0131`=99, `014E`=45; stored `0130`=10, `0131`=100, `014E`=600 (exactly at the thresholds). `codes-conflict` copies the spike's `0100`/`0120`/`0140`/`0101` replies of modules 17 and CB byte for byte (spike L24–70) and CB's `0A` `7F 0A 11` (spike L122–128); `020200` → `42 02 00 00 00` from 17 as phone-console L42.
- Every tx passes `allowedCommand` (42 commands, none refused). `pnpm replay` of each fixture: exit 0, no timeouts, no drops (cleared 14/14 data, permanent 7/7, stored 11/11 with 13 frames, conflict 10/10 with 17 frames). Decoded lines as intended: `components=incomplete` in cleared/permanent/stored, `components=complete` for 17 and CB in conflict; `WARM_UPS`/`CLR_DIST`/`CLR_TIME` values; `dtcs none` / `dtcs P0133` / `dtcs U0158`; CB `negative code=11`; `freeze 0 dtc none` / `freeze 0 dtc P0133`; `freeze 0 AAT 20 degC`; permanent `0120` → `supported 40`, `0140` → `supported 42` (30/31/4D/4E unflagged).
- Labels: generated from that replay output (decoded lines copied, summary exact, `recording_sha256` of the file bytes); `notes` "Synthetic T0.7 'recently cleared' branch: <branch>; no vehicle evidence."
- `observation-labels.test.ts` 11/11; `recordings.test.ts` replays 7 synthetic fixtures. `pnpm check` exit 0 (obd-core 152, mobile 60). `git diff --check` clean; new files have no trailing whitespace. Hardware: none required; NOT RUN.

### B1 round 1 (Claude reviewer, 2026-09-24): APPROVE

- 6/6 B1 items. Every fixture byte was hand-decoded and sourced. codes-conflict matches spike L24–70 and L127 and phone-console L42–44 byte for byte. The labels match the replay output and the SHA-256s. Pre-existing replays are byte-identical. `pnpm check` PASS (obd-core 152). All B2 branches can be reached, and codes-stored tests the at-or-above boundary on all three counters. All deviations are accepted; codes-stored without bitmaps is the better choice (failure 12).
- Minors (two record-hygiene issues: the escape wording, three "Next action" sections) were CLOSED by the orchestrator.

## B2 implementation

2026-09-24, Claude implementer. Files: `packages/obd-core/src/report/codes.ts`, `packages/obd-core/src/report/render.ts`, `packages/obd-core/scripts/codes-report.ts`, `packages/obd-core/test/codes-report.test.ts`, `packages/obd-core/test/codes-reports.md` (generated), `docs/ARCHITECTURE.md`. Root `package.json` NOT touched (orchestrator instruction: T0.6 merge pending, and T0.6 edits that file), so the `codes-report` script is not registered yet.

- Test first: `test/codes-report.test.ts` (artifact equality, failures 1–9, 11, 12 from the recordings and fixtures, isolated failure 10) failed on the current source because `scripts/codes-report.ts` and `src/report/` did not exist.
- Code: builder and legs per spec Builder rules; `LOW_COUNTER` = 30: 10, 31: 100, 4E: 600 (POLICY, Decisions 1, commented); verdict per Decisions 2–3. Renderer uses the exact verdict sentences and status words; one layout choice: "1 module" (singular) in the header line.
- Artifact: `node --import tsx packages/obd-core/scripts/codes-report.ts <PATHS> > packages/obd-core/test/codes-reports.md`, exit 0. Read against the spec: spike and spike-2 legs yes/no/unknown/no, unknown; phone console yes/unknown/unknown/unknown, unknown, module 17 "none stored"; cleared indicated (no AAT line); permanent indicated strong, counters unknown; stored not indicated, freeze "stored for P0133; AAT 20 degC", 7E9 not read; conflict unknown ("checks disagree"), CB "not answered (negative response 11)". Regeneration piped to `diff`: empty, exit 0.
- Mutations (reverted, `cmp` clean): `<` → `<=` fails the artifact and failure 9; "not indicated" from any `no` leg fails the artifact, failures 1–3, 4 and 10.
- Purity grep empty; `src/report/` imports only `../elm` (types), `../obd`, and `./codes`. No file used by `pnpm replay` changed, so replay output is unchanged.
- `pnpm check` exit 0 (obd-core 162, mobile 60). `git diff --check` clean; new files have no trailing whitespace. Hardware: none required; NOT RUN.

### B2 round 1 (Claude reviewer, 2026-09-24): REQUEST_CHANGES

- 7/7 items. Artifact regenerated byte-identical; replay unchanged; buyer read clean (no "clean", thresholds labelled policy, no VIN, "not indicated" only in codes-stored). Five of six mutants caught.
- 1. [blocking] Decision 3 is untested: the mutant "strong without an incomplete monitor" survives, and that is the likely Equinox field case. Spec amended with failure 13 → repair round 1.
- 2. [minor] `pnpm codes-report` is named before the script exists (A2 checklist).
- 3–4. [minor, accepted] `frameNo` in readings; value imports from `obd/` (spec wording corrected).

### B2 repair round 1 (Claude implementer, 2026-09-24)

- Test only: `packages/obd-core/test/codes-report.test.ts` gains "recentlyCleared strong path (isolated, failure 13)", one `it.each` over `monitorsIncomplete` "no" and "unknown" with `noStoredDtcs:"yes", countersLow:"unknown", permanentDtcs:"yes"`; expects `{verdict:"unknown", strong:false, legs}`. The input shape matches `ClearLegs` in `src/report/codes.ts` as is.
- Mutant `strong = noStoredDtcs==="yes" && permanentDtcs==="yes"` (monitor check dropped) applied to `src/report/codes.ts` with the original saved in the session scratchpad: both failure 13 cases fail (2 failed, 10 passed); every other test in the file still passes, confirming the reviewer's survivor. Restored from the copy, `cmp` clean; failure 13 passes on the current code.
- No source change. `src/report/codes.ts` and `test/codes-reports.md` SHA-256 unchanged before and after (`sha256sum -c` OK); artifact equality test passes.
- `pnpm check` exit 0 (obd-core 164, mobile 60). `git diff --check` clean; the new test lines have no trailing whitespace. Temps deleted. Hardware: none required; NOT RUN.

### B2 re-review (Claude reviewer, 2026-09-24): APPROVE

- Failure 13 at `codes-report.test.ts:186-191`; the mutant is now killed (2 failed, only the failure-13 cases). `src/report/` is unchanged; the artifact regenerates byte-identically; `pnpm check` PASS. Hashes after the round: `codes.ts` 6125e049…53e5, `render.ts` 67bf0578…7d14, `codes-reports.md` 8cc85400…a7d14.

## Next action

- A1, B1, B2 closed; the owner commits. A2 after the T0.6 merge: the §Write safety doc edit (re-check DS p.32), the `guard.ts` comment and helper inline (A1 findings 1–2), the root `package.json` `codes-report` script, and correcting the spec Goal wording. Then T0.7 closes.
