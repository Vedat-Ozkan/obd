# T0.1 task run

## Ownership

- Last tool: Claude (took over from Codex 2026-09-16; Codex work stopped, no active process)
- Last role: reviewer (APPROVE)
- Current stage: post-commit gates. Initial commit `afbdb4e` pushed to origin/main; first CI run FAILED at job setup; fix committed, awaiting second run and clean-clone check
- Completed stages: preflight, architecture, T0.1a implementation and local checks, T0.1b implementation and local checks
- Other active tool or role: none

## Contract

- Task: T0.1 — scaffold the monorepo defined in `docs/PLAN.md`
- Spec paths: `docs/specs/T0.1a-typescript-workspace.md`, `docs/specs/T0.1b-hil-bridge-ci.md`
- Approved decisions: automatic architect → implementer → reviewer workflow; routine scope confirmation waived
- Repair count: 1 of 2 (used)
- Escalation count: 0
- Escalation reason: none

## Baseline

- Git state at start: repository has no tracked files; every existing file is untracked
- Existing project files: `.gitignore`, `AGENTS.md`, `CLAUDE.md`, `CODEX.md`, `README.md`, `.agents/`, `.claude/`, `.codex/`, and `docs/`
- Existing specs: `docs/specs/README.md`, `docs/specs/X-2026-09-16-codex-workflow.md`
- T0.1 spec, manifests, packages, workflows, and bridge files did not exist
- Tooling: Node `v22.13.0`; pnpm `9.15.9`; Python `3.10.12`; `uv` unavailable on PATH; git `2.49.0`
- Preserve all pre-existing files and Claude/Codex workflow setup

## Touched files

- `docs/task-runs/T0.1.md` — task handoff record
- `docs/specs/T0.1a-typescript-workspace.md` — architect-authored TypeScript workspace contract
- `docs/specs/T0.1b-hil-bridge-ci.md` — architect-authored bridge and CI contract
- `package.json` — T0.1a root manifest and scripts
- `pnpm-workspace.yaml` — T0.1a workspace globs
- `tsconfig.json` — T0.1a strict ESM TypeScript configuration
- `eslint.config.js` — T0.1a TypeScript lint configuration
- `pnpm-lock.yaml` — generated T0.1a dependency lock
- `packages/obd-core/package.json` — empty package manifest
- `packages/obd-diagnose/package.json` — empty package manifest
- `packages/obd-eval/package.json` — empty package manifest
- `.github/workflows/check.yml` — T0.1b CI workflow
- `tools/hil-bridge/pyproject.toml` — T0.1b Python package and tool configuration
- `tools/hil-bridge/uv.lock` — generated T0.1b dependency lock
- `tools/hil-bridge/src/hil_bridge/__init__.py` — T0.1b package marker
- `tools/hil-bridge/src/hil_bridge/app.py` — T0.1b hello-world FastAPI app and entry point
- `tools/hil-bridge/tests/test_app.py` — T0.1b HTTP-level test

## Verification evidence

- `git status --short` — PASS: baseline captured; all existing files are untracked
- `rg --files --hidden -g '!.git'` — PASS: no T0.1 implementation exists
- Tool version checks — PASS with versions recorded above; `uv --version` — NOT RUN because command is unavailable
- Architecture — PASS: T0.1 split into two ordered specs with 8 and 7 implementation files; dependencies and verification commands are explicit
- `pnpm install` — PASS (T0.1a implementer)
- `pnpm install --frozen-lockfile` — PASS (T0.1a implementer)
- `pnpm typecheck` — PASS (T0.1a implementer)
- `pnpm lint` — PASS (T0.1a implementer)
- `pnpm test` — PASS: all three packages explicitly report empty Vitest suites
- `pnpm -F obd-core test` — PASS: explicit no-tests result
- `pnpm check` — PASS for the T0.1a TypeScript-only gate; T0.1b must extend and rerun it
- `uv lock --check` — PASS (interrupted T0.1b implementer)
- `uv sync --frozen` — PASS with managed Python `3.11.16` (interrupted T0.1b implementer)
- `uv run ruff check .` — PASS (interrupted T0.1b implementer)
- `uv run pytest` — PASS: 1 test; 2 upstream deprecation warnings

## Review findings

Review 1 (Claude reviewer, 2026-09-16): REQUEST_CHANGES. Reviewer reproduced `pnpm install --frozen-lockfile`, `pnpm check`, all four `uv` checks, the loopback curl, and reran the clean-copy check: all PASS. Spec coverage 20/23.

- [blocking] `.github/workflows/check.yml`: `actions/setup-node@v7` with `cache: pnpm` runs before `pnpm/action-setup@v4`; setup-node runs `pnpm store path` and hosted runners lack pnpm, so the job fails before any install. Fix: move `pnpm/action-setup` above `setup-node`.
- [minor] Action refs not recorded as the T0.1b spec requires. Refs in the file: `actions/checkout@v7`, `actions/setup-node@v7`, `pnpm/action-setup@v4`, `actions/setup-python@v7`, `astral-sh/setup-uv@v10`. Unverified offline; confirm on first push.
- [minor] `tsconfig.json` `allowJs` + `eslint.config.js` include is a necessary workaround (include-only fails TS18003, `"files": []` fails TS18002 under tsc 6.0.3) but lacks a why comment; remove the pair once the first package `.ts` lands.
- [minor, optional] Bridge binds `127.0.0.1:8000`; docs promise `<laptop-ip>:8765`. Per T0.1b spec; T0.6 changes both.
- [minor, optional] `eslint.config.js` ignores omit `tools/hil-bridge/.venv/`; no JS there today.
- [minor, no action] Empty `fixtures/recordings/`, `fixtures/synthetic/` dirs are invisible to git and harmless.

Review 2 (Claude reviewer, 2026-09-16, after repair round 1): APPROVE. Reviewer reproduced `pnpm install --frozen-lockfile`, `pnpm check`, `pnpm -F obd-core test`, and all four `uv` checks: PASS. Blocking finding fixed (step order verified at `check.yml:12-18`); tsconfig why-comment and action refs verified. No other file changed (same 40 entries). No OBD constants; hard rules clean; test exercises the real app, nothing mocked. Spec coverage 21/23; the two unmet items are the post-commit gates. Remaining minors are optional and owned by T0.6 (bind/port) or moot (eslint ignores).

## Claude reconciliation (2026-09-16)

- Reproduced the incident: `pnpm check` FAILS at `tsc` with `Unable to resolve @typescript/typescript-linux-x64`; `package.json` and `pnpm-lock.yaml` are at `typescript ^7.0.2`; lockfile records `typescript-eslint@8.70.0` peer `typescript >=4.8.4 <6.1.0`.
- Bridge evidence re-verified: `uv lock --check`, `uv sync --frozen`, `uv run ruff check .`, `uv run pytest` all PASS (1 test). `uv` 0.12.15 is now on PATH at `~/.local/bin/uv` (the baseline said absent); uv-managed CPython 3.11.16 present.
- Registry check: latest TypeScript 6.x is `6.0.3`, inside the peer range.
- Not yet run by anyone: `uv run hil-bridge` loopback check, `pnpm install --frozen-lockfile` after repair, the clean-copy check, and the independent review.

## T0.1b recovery (Claude implementer, 2026-09-16)

- `pnpm add -D -w typescript@6.0.3` — PASS: `package.json` now `^6.0.3`; lockfile regenerated, no `typescript@7` entries
- `pnpm install --frozen-lockfile` — PASS
- `pnpm check` — PASS: tsc, eslint, 3× vitest (explicit no-tests), ruff, pytest all visible, exit 0
- `cd tools/hil-bridge && uv run hil-bridge` + `curl http://127.0.0.1:8000/` — PASS: exact JSON, HTTP 200; server stopped, port released (software only)
- Pre-commit clean-copy check (scratchpad copy, frozen installs, `pnpm check`) — PASS (clean-copy, not clean-clone)
- Workflow inspection — PASS with a caveat raised for review: `setup-node` with `cache: pnpm` precedes `pnpm/action-setup`; likely CI failure "Unable to locate executable file: pnpm"
- `git status --short --untracked-files=all` — PASS: exactly the 15 touched files plus pre-existing setup (orchestrator's handoff-protocol edits excluded)
- Clean-clone — NOT RUN: no commit exists
- Hosted CI — NOT RUN: no push exists
- Noted: empty `fixtures/synthetic/` and `fixtures/recordings/` directories exist (pre-T0.1a, not in either spec, invisible to git)

## Repair round 1 (Claude implementer, 2026-09-16)

- `.github/workflows/check.yml`: `pnpm/action-setup@v4` moved above `actions/setup-node@v7`; nothing else changed. Action refs recorded: `actions/checkout@v7`, `actions/setup-node@v7`, `pnpm/action-setup@v4`, `actions/setup-python@v7`, `astral-sh/setup-uv@v10` (unverified against hosted runners until first push).
- `tsconfig.json`: two JSONC why-comment lines above `allowJs` (TS18003/TS18002 reason; remove with first package `.ts`).
- `pnpm install --frozen-lockfile` — PASS; `pnpm check` — PASS (all six stages visible); `git status --short --untracked-files=all` — PASS, same 40 entries, no new files.
- Clean-copy and loopback — NOT RERUN (unaffected by a comment and a step reorder). Clean-clone, hosted CI — NOT RUN (no commit/push).

## Implementation incident

- During T0.1b validation, `package.json` and `pnpm-lock.yaml` changed from the verified TypeScript `^6` state to TypeScript `^7.0.2`, matching the earlier superseded unconstrained pnpm resolution.
- The active implementer was interrupted before further writes. Process inspection found no active Claude, pnpm, or bridge process.
- The TypeScript 7 lock conflicts with `typescript-eslint`'s declared `<6.1.0` peer range and caused `pnpm check` to fail on a missing platform package.
- Recovery must restore the architect-approved compatible TypeScript 6 range, regenerate the pnpm lockfile, preserve the completed T0.1b files, and rerun all affected checks.

## Post-commit gates

- Hosted CI run 1 (commit `afbdb4e`) — FAIL: `Unable to resolve action astral-sh/setup-uv@v10, unable to find version v10`. Verified with `git ls-remote --tags`: setup-uv majors go to `v7`; checkout/setup-node/setup-python `v7` and pnpm/action-setup `v4` exist. Fixed `check.yml` to `astral-sh/setup-uv@v7` (orchestrator, one-line ref fix; reviewer had flagged the refs as unverified offline).
- Hosted CI run 2 — pending push.
- Clean-clone — pending.

## Blocker

- Literal clean-clone and hosted-CI gates remain unavailable until a user-authorized commit and push. (TypeScript 7 conflict repaired 2026-09-16.)

## Next action

- User decides on the initial commit and push. After the push: clone the commit to a temp dir, `pnpm install --frozen-lockfile`, `cd tools/hil-bridge && uv sync --frozen`, `pnpm check`; inspect the hosted `check` run. Both PASS → tick T0.1 in `docs/PLAN.md`. Then `/feature T0.2` (needs a laptop/car session).
