# T0.8 task run

## Ownership

- Last tool: Claude (took over 2026-09-23 after Codex hit its usage limit mid-T0.8b)
- Last role: reviewer (round 3: APPROVE, 2026-09-23)
- Current stage: CLOSED 2026-09-24 (Equinox phone acceptance via the T0.8d one-button capture)
- Completed stages: preflight, architecture (two ordered specs and one bounded T0.8a revision), scope confirmation through user decisions, T0.8a local implementation and local checks
- Other active tool or role: none besides the Claude T0.8b implementer (Codex stopped at its usage limit; its T0.8b implementer produced no report)

## Contract

- Task: T0.8 — Android Expo development build, Veepeak BLE scan/connect, `BleTransport`, debug console, and standard `.jsonl` recording export.
- Spec paths: `docs/specs/T0.8a-expo-android-development-build.md`, `docs/specs/T0.8b-ble-console-recording.md`
- Approved decisions: task selected by user 2026-09-23; routine scope authorization supplied by the workflow. ADR-015 makes the Equinox EV the required T0.8 phone check: one connection, `0100`, and one exported recording that replays. Chrysler and Elantra are optional bench cars; no ICE capture is required now or for later Phase 0 gates. User chose Android application ID `com.ev.obd` and authorized EAS Build source upload and Expo-managed Android signing keystore generation/storage. Expo account ownership may still need selection at build time.
- Repair count: 2 of 2 (last allowed)
- Escalation count: 0; reason: none

## Baseline

- `main` at `be79fb0` (`Close T0.3 and tick T2.3 follow-ups`); `git status --short --untracked-files=all` empty.
- `apps/mobile/` does not exist. `pnpm-workspace.yaml` already includes `apps/*`; T0.3 `Transport` and `ElmLineReader` exist in `packages/obd-core/src/`.
- T0.2 tracked Equinox spike recordings exist. No Chrysler 200 or Elantra recording exists in `fixtures/recordings/`; their baseline tables in `docs/spike-2026-09.md` still say NOT RUN. ADR-015 removed those captures from the completion gates.

## Touched files

- `docs/task-runs/T0.8.md` (orchestrator). User-authorized ADR-015 roadmap revision also touched `AGENTS.md`, `README.md`, `docs/DECISIONS.md`, `docs/ELM327.md`, `docs/EVAL.md`, `docs/FEASIBILITY.md`, `docs/PLAN.md`, `docs/spike-2026-09.md`, and the T0.2/T0.3 task records. Architect added the two T0.8 specs. T0.8a implementer touched exactly its ten spec files (root `package.json`, `tsconfig.json`, `eslint.config.js`, `pnpm-lock.yaml`, `packages/obd-core/package.json`, and five `apps/mobile` files).

## Verification evidence

- Preflight only: `git status --short --untracked-files=all` PASS (clean); `git log -2 --oneline` confirmed baseline. No product checks run yet.
- ADR-015 scope check: `rg --files fixtures/recordings` found only the two tracked Equinox spike files; no ICE recordings. `rg` of their tx lines shows `0101`, `0902`, `03`, `07`, and `0A`; no `0141` in tracked recordings. `git diff --check` PASS after the roadmap edits. This does not establish any new hardware result.
- Architect spec-only pass: two ordered specs, ten T0.8a files and six T0.8b files; no product checks run. The console AT input is limited to eight exact commands sourced to `docs/ELM327.md` and rejects unknown AT commands.
- Initial T0.8a implementation report: `pnpm install --frozen-lockfile` PASS; mobile typecheck/test PASS; `pnpm check` PASS (mobile included, core 42 tests, bridge Ruff and pytest 1 test); `git diff --check` PASS. `pnpm -F mobile exec expo install --check` FAIL because SDK 57 expects `react-native@0.86.3`, while the spec pins `0.86.0`. The exact `pnpm --dir apps/mobile dlx expo-doctor@1.20.4` command FAILS to detect the project; running from `apps/mobile` reports 19/21 checks passing and confirms the RN mismatch. `pnpm -F mobile exec expo config --type prebuild --json` PASS for `com.ev.obd`, but its evaluated config does not show `BLUETOOTH_SCAN`; official Expo config-plugin documentation says AndroidManifest mods run during prebuild, so this may be the wrong check rather than a plugin defect. EAS init/build and phone test NOT RUN: `pnpm dlx eas-cli@24.7.0 whoami` says `Not logged in`; user has been asked to log in locally without sharing credentials.
- Architect revision: T0.8a spec now pins Expo-required `react-native@0.86.3`, runs Expo Doctor from `apps/mobile`, and checks generated AndroidManifest mods with read-only `expo config --type introspect --json`. Architect ran the exact introspection command: PASS for `com.ev.obd`, `BLUETOOTH_CONNECT`, `BLUETOOTH_SCAN` with `tools:targetApi` 31, and location permissions; no native directory generated. `git diff --check` PASS. T0.8b spec unchanged.
- Revised T0.8a implementer PASS: frozen install; Expo dependency check; Expo Doctor 21/21; AndroidManifest introspection (`com.ev.obd`, CONNECT/SCAN, target API 31, coarse/fine location); mobile typecheck and empty-suite test; `pnpm check` (mobile checks, core 42 tests, bridge Ruff/pytest 1 test); single React 19.2.3 and RN 0.86.3 tree; `git diff --check`.
- EAS authentication PASS as `@kemafrost`; `eas init` created `@kemafrost/obd`, project ID `cf8670a2-2c7b-4f03-b463-0393cf7708a7`. Authorized Android development build `0f877dca-b588-43e6-9613-4f86511348d8` was submitted with managed keystore and was IN_QUEUE at handoff: `https://expo.dev/accounts/kemafrost/projects/obd/builds/0f877dca-b588-43e6-9613-4f86511348d8`. APK/phone placeholder NOT RUN yet. During submission, `app.json` acquired explicit BLUETOOTH/ADMIN/CONNECT permissions not present in the spec or earlier file inspection; the implementer removed them from local source. The uploaded archive predates cleanup and is diagnostic only until source/build equivalence is resolved or a clean rebuild succeeds.
- Read-only EAS status query at T0.8b handoff: first Android development build remains `IN_QUEUE`; project identifier, application ID `com.ev.obd`, and profile `development` match. Architect amended T0.8b's bounded file list to include `apps/mobile/package.json` for the required test-script update. Architect also ruled the first build may count only after success and proof its archive retained the BLE plugin and differed from final source solely by equivalent EAS-inserted Bluetooth permissions; otherwise submit a clean-source rebuild.

### Claude reconciliation (2026-09-23)

- Codex's T0.8b implementer stopped without a report when Codex hit its usage limit. On disk: all seven T0.8b files exist (`App.tsx` 98 lines, `src/ble/BleTransport.ts` 83, `src/console.ts` 78, `src/recording.ts` 55, `test/BleTransport.test.ts`, `test/console-recording.test.ts`, `package.json`). Their completeness and checks are unverified, so T0.8b implementation is treated as incomplete; a fresh Claude implementer reconciles and finishes it against the spec.
- EAS build `0f877dca-b588-43e6-9613-4f86511348d8`: status **finished** (started 13:05, finished 14:28), commit `be79fb0`, fingerprint `38c279f8…`, APK `https://expo.dev/artifacts/eas/MpAw3odoSwQhERyUwnGxXyZXEpdC-1N07x0tDEnPP-g.apk`. It is the pre-cleanup upload; per the architect's ruling it counts only after proof it retained the BLE plugin and differs from final source solely by equivalent EAS-inserted Bluetooth permissions, otherwise a clean rebuild. Not yet resolved.
- Unrelated uncommitted work in the tree from another Claude session: `.claude/agents/*` (effort lines, two new agents) and `CLAUDE.md` (subagent list). Not part of T0.8.

### T0.8b implementation (Claude implementer, 2026-09-23)

- Reconciled the Codex draft: changed `App.tsx`, `src/ble/BleTransport.ts`, `src/console.ts`, both tests; `package.json` and `src/recording.ts` already correct. Safety/order fixes: `recording.tx` before pending state and before `transport.write`; every notification recorded as rx; scan de-dup; disconnect path.
- `pnpm install --frozen-lockfile` PASS; `pnpm -F mobile test` PASS (47); `pnpm -F mobile typecheck` PASS; `npx eslint apps/mobile` PASS; `pnpm check` PASS; `cd apps/mobile && pnpm dlx expo-doctor@1.20.4` PASS 21/21 (spec's `--dir` form FAILS as known from T0.8a); prebuild and introspect config PASS; static safety grep PASS; `git diff --check` PASS; recordings untouched PASS. Phone + Equinox session NOT RUN.
- Deviations flagged: expo-doctor `cd` form; CR/LF/TAB rejected; Disconnect button + transcript lines; test imports `ReplayTransport` by relative path. Open questions: stray untracked root `app.json` (`{"expo": {}}`, predates this session); spec's expo-doctor wording; obd-core export of ReplayTransport.

### Repair round 1 (Claude implementer, 2026-09-23)

- Root `app.json` (`{"expo": {}}`) deleted; `BleTransport.ts`: post-connect steps in `setUpVeepeak`, cancel once on any failure without masking the original error; write role requires exactly one candidate; +3 tests.
- `pnpm -F mobile test` PASS (50); typecheck PASS; `npx eslint apps/mobile` PASS; `pnpm check` PASS; `git diff --check` PASS (plus `--no-index` on untracked edits); `git status` (root `app.json` gone) PASS; introspect PASS. Hardware NOT RUN. Deviations: none.

## Review findings

### Round 1 (Claude reviewer, 2026-09-23): REQUEST_CHANGES

1. [blocking, CLOSED round 2] stray untracked root `app.json` (`{"expo": {}}`, mtime 14:47) fails the git-status items of both specs → remove.
2. [major, CLOSED round 2] `apps/mobile/src/ble/BleTransport.ts:32-50` — `connectVeepeak` never cancels the connection when a step after `connectToDevice` throws; dongle stays connected, retries fail → try/catch, `cancelConnection`, rethrow, plus a test.
3. [minor, CLOSED round 2] `BleTransport.ts:41` — two write-without-response characteristics fall back to a guessed write-with-response role instead of failing as ambiguous.
4. [minor, closed by orchestrator] T0.8b spec's expo-doctor `--dir` line superseded by the `cd` form (note appended to the spec).
5. [minor, closed by orchestrator] SDK version and equivalence ruling recorded below.
- Deviations accepted (expo-doctor `cd` form; CR/LF/TAB rejected; Disconnect button and transcript lines; relative ReplayTransport import in the test). Safety review, BleTransport behavior, purity and dependencies: hold. `pnpm check` PASS; 14/21 items satisfied, 4 hardware-only NOT RUN, 3 unmet (items tied to findings 1 and 5).
- **EAS build `0f877dca-b588-43e6-9613-4f86511348d8` ruling: COUNTS, no rebuild.** sdkVersion 57.0.0, appIdentifier `com.ev.obd`, profile development, commit `be79fb0`, fingerprint `38c279f89172ba57610c35fe646f8e3d00fb9dfb`. Build log READ_APP_CONFIG keeps `react-native-ble-plx` `{isBackgroundEnabled:false, neverForLocation:false}` with permissions BLUETOOTH, BLUETOOTH_ADMIN, BLUETOOTH_CONNECT; the plugin-only source evaluates to the same, so the removed explicit entries were redundant. Local `@expo/fingerprint` 0.20.13 reproduces `38c279f8…` exactly with only the `test` script reverted to T0.8a's `vitest run --passWithNoTests`, so native config, autolinking and eas.json are identical; T0.8b changed only JS and the test script. The T0.8a phone placeholder check is superseded by T0.8b hardware step 1.

### Round 2 (Claude reviewer, 2026-09-23): APPROVE

- Findings 1–3 fixed; new tests would fail against the old code; repair touched only `BleTransport.ts` and its test. No new findings.
- Reran: frozen install, `expo install --check`, expo-doctor 21/21 (`cd` form), introspect and prebuild config, mobile typecheck/eslint/tests (50), single React 19.2.3 / RN 0.86.3, `git diff --check` incl. untracked, fixtures unchanged, no secrets, `pnpm check` PASS. 18/22 items satisfied, 4 hardware-only NOT RUN.
- Safety audit on current code: single `transport.write` (console.ts:51) after busy check → validation → `recording.tx` → pending; exact allowlist; control characters rejected.
- Not reproduced by the reviewer: EAS query (outside its command set); relies on the recorded ruling.

### Phone session attempt (user, 2026-09-23): FAIL

- APK from build `0f877dca` installed; `pnpm -F mobile start` served the dev client; the app failed to open: `Android Bundling failed … Unable to resolve "./src/ble/BleTransport.js" from "apps/mobile/App.tsx"`. Cause: Metro does not map NodeNext `.js` specifiers to `.ts` sources; `packages/obd-core/src` uses the same style internally. No local check ran Metro, so all earlier gates missed it.
- Reproduced offline by the orchestrator: `cd apps/mobile && pnpm exec expo export --platform android --output-dir /tmp/claude-1000/obd-export-before` → `Error: Unable to resolve module ./src/ble/BleTransport.js`.
- Orchestrator amendment appended to the T0.8b spec: add `apps/mobile/metro.config.js` with a `.js` → `.ts` resolver; new verification item `expo export --platform android` exits 0.

### Repair round 2 (Claude implementer, 2026-09-23)

- Created `apps/mobile/metro.config.js` (default Expo config plus a `.js` → `.ts`/`.tsx` `resolveRequest` for relative specifiers, falling back to default resolution); no other file changed.
- `cd apps/mobile && pnpm exec expo export --platform android --output-dir /tmp/claude-1000/obd-export-after` PASS (709 modules; bundle contains strings unique to `packages/obd-core/src/recording/format.ts`); export wrote nothing tracked inside the repo (it updates the gitignored `apps/mobile/.expo/dev/logs/export.log`); `pnpm -F mobile test` PASS (50); typecheck PASS (tsconfig does not cover the .js file); `npx eslint apps/mobile` PASS (no rules applied to .js); `pnpm check` PASS; `git diff --check` PASS. Phone NOT RUN.

### Legibility amendment (Claude implementer, 2026-09-23; owner request)

- `apps/mobile/App.tsx` only: `useColorScheme()` palettes (light: bg #FFFFFF, text #111111, console #000000 on #F2F2F2; dark: bg #121212, text #EDEDED, console #E6E6E6 on #000000), explicit colors on background, Text, TextInput, placeholders, borders, console; transcript in `monospace`.
- WCAG text contrast PASS (lowest 6.38:1, dark placeholder); borders ≥ 4.06:1. `pnpm -F mobile test` PASS (50); typecheck PASS; eslint PASS; `expo export --platform android` PASS; `pnpm check` PASS; `git diff --check` PASS; no new files. Phone visual check NOT RUN.

### Round 3 (Claude reviewer, 2026-09-23): APPROVE

- 17/17 local items PASS incl. `expo export --platform android` (709 modules; bundle contains console safety strings and the palettes); "failed before" confirmed by reading metro-resolver 0.84.6 (appends extensions, never swaps `.js` → `.ts`). Fallback chaining correct; only relative `.js` specifiers rewritten; no monorepo override. Contrast recomputed and matches. Safety audit redone: console.ts unchanged, single write path, exact allowlist. APK from build `0f877dca` still valid (JS/bundler-only changes).
- Minor findings (open): 1. `metro.config.js:13-15` bare `catch {}` swallows non-resolution errors from the `.ts` attempt (misleading message only). 2. `metro.config.js:10` rewrite also applies inside node_modules (no current package affected). 3. `App.tsx:126-133` native Buttons keep Android's default white on #2196F3 = 3.12:1, below the 4.5:1 target (not in the amendment's list). 4. `App.tsx:26` `#F2F2F2` trips the static safety grep for `2F` (false positive). 5. Record wording on export side effects (fixed above).

## Blocker

- No local gate blocker. T0.8a needs a successful source-matched EAS APK and physical-phone placeholder check. The queued first build has an unexplained pre-cleanup permission delta; resolve it before approval. T0.8b vehicle acceptance still needs the owner-arranged Equinox phone session.

## Next action

- Owner: restart `pnpm -F mobile start`, confirm the dev client loads and is legible in dark mode, then the Equinox session (ATZ/ATE0/ATL0/ATS0/ATH1/ATSP0/0100, export to `fixtures/recordings/chevrolet-equinox-ev-2024/2026-09-<dd>-phone-console.jsonl`); then `/feature T0.8` to reconcile and tick.
- Optional follow-up task for minor findings 1–4 (button contrast is the user-visible one).

## Codex handoff: minor mobile follow-ups (2026-09-23)

- Last tool and role: Codex orchestrator; current stage: architecture for the user-requested follow-up. Previous T0.8 software review remains approved; its Equinox phone acceptance is pending. Claude is working on T0.5 in different files, with no active T0.8 role reported.
- Completed stages for this follow-up: preflight and reconciliation of this record, the T0.8b spec, the mobile files, and the working tree. The prior T0.8 architecture, implementation, reviews, and EAS evidence above remain historical evidence; this follow-up will receive its own spec and independent review.
- Goal / approved scope: address round-3 minor findings 1–4 in `apps/mobile/App.tsx` and `apps/mobile/metro.config.js`: button text contrast, resolver catch behavior and scope, and the light console background safety-grep false positive. No dependency, native config, protocol, recording, or T0.5 changes.
- Baseline `git status --short`: ` M docs/PLAN.md`, ` M docs/task-runs/T0.4.md`, `?? docs/task-runs/T0.5.md`. Those are other work and must be preserved. `apps/mobile/App.tsx` and `apps/mobile/metro.config.js` are clean at handoff.
- Spec path: pending architect. Touched so far: this progress record only. Verification for the follow-up: NOT RUN, architecture has not finished.
- Prior T0.8 repair count: 2 (rounds 1 and 2 above); no follow-up repair yet. Escalation count: no escalation recorded in the imported T0.8 history; follow-up escalation count 0. Remaining review findings: round-3 minor findings 1–4 above. Blocker: none for local work. Next action: architect writes a bounded follow-up spec, then implementer and independent reviewer run sequentially.

### T0.8c architecture handoff

- Last tool and role: Codex architect completed; Codex orchestrator handing off to implementer. Current stage: implementation. Completed follow-up stages: preflight, architecture, routine scope confirmation supplied by user request and workflow. No material open questions.
- Spec: `docs/specs/T0.8c-mobile-follow-up.md`. Goal: close four minor findings in the two mobile files. Non-goals: BLE/protocol/recording changes, dependencies, native configuration, T0.5. Files: `apps/mobile/App.tsx`, `apps/mobile/metro.config.js` only; architect added the spec. Approved decisions: `#005A9C` on all six buttons, `#F4F4F4` light console, narrow Metro missing-path handling, no rewrites for `node_modules` importers. No new hardware check or dependency.
- Architecture verification: `git diff --check` PASS for spec. Implementation and independent review checks NOT RUN yet. Previous T0.8 repair count remains 2; no new repair or escalation. Next action: implementer edits only the two authorized mobile files and reports exact verification results.

### T0.8c implementation handoff

- Last tool and role: Codex implementer completed; Codex orchestrator handing off to fresh independent reviewer. Current stage: review. Completed follow-up stages: preflight, architecture, implementation. Spec remains `docs/specs/T0.8c-mobile-follow-up.md`; touched implementation files: `apps/mobile/App.tsx`, `apps/mobile/metro.config.js` only.
- Implementer verification PASS: `git diff -- apps/mobile/App.tsx apps/mobile/metro.config.js` (bounded edits, six colored buttons); WCAG calculation in spec (button 7.14:1, light console text 19.09:1, border 4.13:1); `! rg -n '2F' apps/mobile/App.tsx`; spec's Node resolver harness (candidate order, `.js` fallback, error identity, POSIX/Windows dependency paths); `pnpm -F mobile test` (50); `pnpm -F mobile typecheck`; `pnpm lint`; `cd apps/mobile && pnpm exec expo export --platform android --output-dir /tmp/obd-t08c-export.laBU4E`; `git diff --check`; `git status --short --untracked-files=all` (other task edits preserved). Deviations: none.
- Implementer `pnpm check`: NOT RUN to completion; process was terminated after the execution environment's 30-second command window during package tests, without failure output. Reviewer must rerun it independently with sufficient time. Hardware: NOT RUN; original T0.8 Equinox phone gate is pending and this follow-up has no new hardware check.
- Remaining review findings pending fresh review: prior four minor findings are addressed in the implementation but not independently approved. Previous T0.8 repair count 2; no T0.8c repair or escalation. Blocker: none yet. Next action: reviewer independently checks the spec and entire T0.8c diff, reruns required checks, and returns APPROVE or REQUEST_CHANGES.

### T0.8c independent review and close

- Last tool and role: Codex independent reviewer returned APPROVE; Codex orchestrator recorded handoff and closed the local follow-up. Completed stages: preflight, architecture, implementation, independent review. Spec: `docs/specs/T0.8c-mobile-follow-up.md`. Task-touched files: that spec, `apps/mobile/App.tsx`, `apps/mobile/metro.config.js`, and this record. The concurrent T0.5 work later added untracked `docs/specs/T0.5-standard-decoding.md`; it and the baseline T0.4/T0.5 documentation remain untouched by this follow-up.
- Reviewer verification PASS: `pnpm check` (87 core and 50 mobile tests, TypeScript, ESLint, bridge Ruff and pytest); spec WCAG calculation (button 7.14:1, light console text 19.09:1, border 4.13:1); spec Node resolver harness (TypeScript candidates, original `.js` fallback, error identity, POSIX/Windows `node_modules` exclusion); `! rg -n '2F' apps/mobile/App.tsx`; six-button color audit; `pnpm -F mobile test` (50); `pnpm -F mobile typecheck`; `pnpm lint`; `cd apps/mobile && pnpm exec expo export --platform android --output-dir /tmp/obd-t08c-review-export.KXVYd2` (bundle and metadata); `git diff --check`; and `git status --short --untracked-files=all` (no repo export artifacts, concurrent edits preserved). Reviewer findings: none. The four earlier minor findings are closed by this approved follow-up.
- Reviewer hardware verification: NOT RUN for the original T0.8 Equinox acceptance, which still requires an owner-arranged phone session: `pnpm -F mobile start`, confirm dev client loads and is legible in dark mode, send `ATZ`, `ATE0`, `ATL0`, `ATS0`, `ATH1`, `ATSP0`, `0100`, export to `fixtures/recordings/chevrolet-equinox-ev-2024/2026-09-<dd>-phone-console.jsonl`, and replay per the T0.8b spec. T0.8c adds no hardware check and does not complete the parent T0.8 task.
- Repair count: historical T0.8 rounds 1 and 2 remain 2; T0.8c follow-up 0. Escalation: none recorded in prior T0.8 history; T0.8c 0. Blocker for local follow-up: none. Next action: owner performs the pending Equinox phone acceptance and hands T0.8 back for reconciliation; do not tick the plan or commit on this approval alone.

- Orchestrator note 2026-09-23 (T0.5 Decision 2): during the Equinox phone session, also send `020000` and `020200` and keep them in the exported recording, so T0.5 Stage C has a real Mode 02 answer to source.

## Hardware acceptance (2026-09-24)

- The owner ran the T0.8d one-button capture on the Equinox. It sent `ATZ ATE0 ATL0 ATS0 ATH1 ATSP0 0100 020000 020200 03` through the dev client (EAS `0f877dca`). `0100` succeeded, and the exported recording `fixtures/recordings/chevrolet-equinox-ev-2024/2026-09-24-phone-console.jsonl` replays green (10 commands, 0 errors, 0 timeouts). The committed copy is `2026-09-24-phone-console.redacted.jsonl`. Details are in `docs/task-runs/T0.8d.md`.
- Orchestrator ruling: this meets the PLAN T0.8 verify column (`0100` from the app, one recording exported and replays green). The capture button replaced typed commands, and each required tx appears once with a complete reply. Dark-mode legibility was not separately confirmed by the owner.
