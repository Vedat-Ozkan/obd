# T0.5 task run

## Ownership

- Last tool: Claude
- Last role: reviewer (Stage C round 1: APPROVE, 2026-09-24)
- Current stage: CLOSED 2026-09-24 (Stages A, B, C)
- Completed stages: preflight, architecture (Claude architect 2026-09-23; spec written although the agent's notification reported a weekly API limit), scope confirmation (owner: "start on stage a", 2026-09-23)
- Other active tool or role: none

## Contract

- Task: T0.5 — `obd-core` standard decoding: supported-PID bitmaps (00/20/40/60/80/A0/C0), Mode 01 PIDs for the codes report and battery context (01, 1C, 21, 30, 31, 41, 42, 46, 4D, 4E) plus standard hybrid/EV PIDs (5B and others in the checked-in J1979 table), Mode 02 freeze frame, Mode 03/07/0A DTC lists with P/C/B/U decoding, Mode 09 VIN and CAL ID, readiness monitors from PID 01 and 41; all formulas in a table-driven J1979 file checked into the repo. Verify (PLAN): table-driven tests with known input bytes → values; DTC decode tests; VIN test from the Equinox recording; `pnpm replay` on the EV spike recording shows the values it actually answered; synthetic fixtures cover other decode branches.
- Spec paths: `docs/specs/T0.5-standard-decoding.md` (Stages A, B, C)
- Approved decisions: owner "go ahead with the next phase" (2026-09-23) after T0.4 closed; ADR-015 (Equinox-only evidence); ADR-017 (committed recordings have the VIN serial masked); spec questions answered as recommended (recordings → OBDb SAEJ1979 → pinned Wikipedia; Mode 02 captured on the T0.8 phone session before Stage C).
- Repair count: 0 of 2
- Escalation count: 0
- Escalation reason: none

## Baseline

- Git state at start: `main` at `9c364ca` (T0.4 committed); `git status --short`: `M docs/PLAN.md` (T0.4 tick, T0.7 prerequisite), `M docs/task-runs/T0.4.md` (orchestrator).
- Inputs: T0.4 `Elm327Session`, ISO-TP reassembly and `pnpm replay`; tracked recordings `fixtures/recordings/chevrolet-equinox-ev-2024/2026-09-22-spike.redacted.jsonl` and `-spike-2.redacted.jsonl` (VIN serial masked with ASCII `0`, so check-digit validation needs a synthetic VIN); local gitignored discovery recordings (2026-09-23) with Mode 01 answers for 01, 0D, 1C, 1F, 21, 30, 31, 41, 42, A6 and Mode 09 infotypes per module.
- Constraint: no car or Bluetooth; replay- and fixture-based.

## Touched files

- `docs/task-runs/T0.5.md` (this record)
- `docs/specs/T0.5-standard-decoding.md` (architect; Decisions by orchestrator)

## Verification evidence

### Stage A implementation (Claude implementer, 2026-09-23)

- 10 Stage A files. Vendored `default.json` and `LICENSE` fetched from OBDb/SAEJ1979 at `d3259214…`; SHA-256 `5bef2c25…c767` and `8cb7fbf0…844c` match the spec.
- `pnpm -F obd-core test` PASS (7 files, 151 tests): table-vs-OBDb cross-check over all 11 rows (mutation of one row → 3 failures, restored); spike/spike-2 bitmaps and `0101` readiness for all five ECUs; synthetic cases. `pnpm check` PASS; purity grep PASS; `git diff --check` PASS; recordings untouched. Hardware NOT RUN by design.
- Deviations flagged: `decodePid` check order (echo → unknown-pid → length); extra readiness cross-check per OBDb `*_SUP` bit; extra J1979-conventions rows; VIN check-digit row describes the method without reproducing CFR table values; A6 formula written in 4-byte form.
- Pending owner action (outward-facing, not done by agents): file the OBDb upstream report for the PID 01 byte-D polarity conflict (ADR-010), then record its URL here.
- Open for Stage B: whether the 49 CFR 565.15 Table III/IV values should be copied into the ELM327.md VIN row so Stage B cites a checked-in copy.

### Stage B implementation (Claude implementer, 2026-09-23)

- Amendment items done: 49 CFR 565.15 Table III/IV values in `docs/ELM327.md` (eCFR versioner API, retrieved 2026-09-23; sample `1G4AH59H45G118341` → 411 mod 11 = 4); C4 wording; README newline. New: `dtc.ts`, `vin.ts`, tests (`dtc`, `vin`, `replay-decode`), `fixtures/synthetic/standard-decoding.jsonl`, `replay.ts` decoded lines, ARCHITECTURE lines.
- `pnpm -F obd-core test` PASS (10 files, 193); `pnpm check` PASS; `git diff --check` PASS; purity grep PASS; recordings untouched.
- `pnpm replay …/2026-09-22-spike.redacted.jsonl` (exit 0, 38 decoded lines, 0 VIN-run hits): bitmaps per ECU (e.g. 17 `01 0D 1C 1F 20`); L62 `0101` all five `readiness pid=01 mil=off dtcs=0 ignition=spark components=complete`; L72 `vin status=masked` for 17 and 28; L84 `ecu-name GWM-Gateway`; L106/L115 `dtcs none` ×5; L122 `negative code=11` for 17 and CB. Summary unchanged: `32 commands; data 15, ok 15, nodata 1, error 1 (buffer-full=1), timeout 0; frames 41; dropped malformed=1, incomplete=4, no-first-frame=1`. Spike-2 same multiset.
- Local discovery replays: exit 0, 48 decoded lines each, 0 invalid, 0 negative; no VIN in decoded or frame lines.
- Deviations: `vin ` prefix on all `0902` failures; `decodeCalIds` length guard; fixture meta fields; VIN row notes Table IV/VI; eCFR API citation. **Process slip (deviation 6):** decoded values from the local discovery files (CAL IDs, odometer, voltages, ECU names; no VIN) were displayed in the implementer's tool output, and the first masked `0902` chunk of the committed spike file was printed (manufacturer-code region only, already in the committed file). No repo impact.

### Stage C implementation (Claude implementer, 2026-09-24)

Failure modes, written before any Stage C code (the spec is not edited; this list stands in for its Stage C Verification list). One isolated synthetic test each in `packages/obd-core/test/freeze.test.ts`; everything else is the E2E replay of `2026-09-24-phone-console.redacted.jsonl` (L38 `020000`, L42 `020200`, whose `00 00` DTC covers "no freeze frame" with real data):
1. A stored DTC in freeze-frame PID 02 is dropped or misdecoded (read as none, or the frame-number byte taken as the DTC's first byte). Test: `42 02 00 01 33` → `P0133`.
2. A `MODE01_PIDS` value read from a freeze frame is shifted by the frame-number byte (scaled from the wrong bytes). Test: `42 42 00 30 D4` → VPWR 12.5 V.
3. Wrong length is accepted (a truncated or overlong reply decoded as a value). Test: `42 02 00 00` (1 DTC byte) → `length`.
4. Wrong frame number is accepted (a reply for another freeze frame paired with this request). Test: `42 02 01 00 00` when frame 0 was requested → `echo`.

Evidence:
- Files: `packages/obd-core/src/obd/freeze.ts` (new: `decodeFreezeDtc`, `decodeFreezePid`, `decodeFreezeSupported`), `packages/obd-core/test/freeze.test.ts` (new), `packages/obd-core/scripts/replay.ts` (`02` rows), `docs/ELM327.md` (§J1979 conventions: Mode 02 reply layout and freeze-frame PID 02 rows). `supported.ts` not touched; bitmap and PID reuse go through `decodeSupported`/`decodePid` on the reply rewritten to `41 <pid> <data>`.
- Tests written first and seen failing (the module was missing, and the baseline replay had no `decoded` lines at L38/L42); after the code: `freeze.test.ts` 5/5 PASS.
- `pnpm replay fixtures/recordings/chevrolet-equinox-ev-2024/2026-09-24-phone-console.redacted.jsonl` exit 0; new lines: L38 `  decoded 17 freeze 0 supported 02 0D 1F 20` (by hand: A=40 → bit 1 → 02; B=08 → bit 12 → 0D; D=03 → bits 30, 31 → 1F, 20), L42 `  decoded 17 freeze 0 dtc none`. Summary unchanged: `10 commands; data 5, ok 5, nodata 0, error 0, timeout 0; frames 12; dropped none`.
- Baseline vs after `pnpm replay` for all 6 tracked `.jsonl` (3 redacted recordings, 3 synthetic): the only diff is those two added lines; every other output is byte-identical. Temp files deleted.
- `00 00` = no freeze frame: Wikipedia `oldid=1374011962` (fetched to a temp dir, since deleted): the Service 01/02 note says a zero Service 02 PID 02 means no snapshot, and the PID 02 row says "decoded as in service 3". Cited in `docs/ELM327.md`.
- `pnpm check` PASS (exit 0; obd-core 12 files / 141 tests, mobile 57, bridge ruff + pytest). `git diff --check` PASS. Purity grep on `freeze.ts` PASS. Recordings untouched.
- Deviations: (1) each decoder takes the requested frame number (`decodeFreezeDtc(frameNo, frame)`, `decodeFreezePid(pid, frameNo, frame)`) so failure mode 4 can be caught as `echo`; the provisional interface had no such argument. (2) Added `decodeFreezeSupported(base, frameNo, frame)` for the orchestrator's PID 00 decision. (3) `replay.ts`: the Mode 01 value formatting moved into a `readingText` helper so the `02` rows reuse it; Mode 01 output is unchanged. (4) Replay text for freeze rows is `freeze <frame#> supported …` / `freeze <frame#> dtc <code|none>` / `freeze <frame#> <id> <value> <unit>`; failures use the generic `negative code=` / `invalid <reason>` with no prefix.
- Hardware: NOT RUN in this session. The prerequisite capture already exists (above).

## Review findings

### Stage A round 1 (Claude reviewer, 2026-09-23): APPROVE

- 6/6 Stage A items PASS; vendored files re-fetched and byte-identical at `d3259214…` (CC-BY-SA-4.0 attribution satisfied); every table row checked against OBDb; drift test fails on any row change (by reasoning); spike tx lines and hand-decoded bitmaps correct; both conflicts resolved per spec; Wikipedia rev 1374011962 verified; purity and scope clean; `pnpm check` PASS (obd-core 151). All five deviations accepted.
- Minor findings, folded into Stage B by spec amendment: README bullet newline; C4 note paraphrase; VIN check-digit table values must be checked in (49 CFR public domain; verified sample VIN sums to 411 → check digit 4).
- OBDb upstream issue for the PID 01 byte-D polarity: owner deferred it (2026-09-23) to a later batch of OBDb fixes.

### Stage B round 1 (Claude reviewer, 2026-09-23): APPROVE

- 11/11 Stage B items PASS. 49 CFR 565.15 Tables III–V re-fetched and compared programmatically (exact); sample and X-variant check digits verified; C4 paraphrase verified; `masked` decided before the check digit; replay prints no VIN (0 hits in both spikes); DTC, CAL ID, ECU-name decoders and fixture frames hand-checked; summary byte-identical to T0.4; purity, deps, recordings clean; `pnpm check` PASS (obd-core 193).
- Minor (no action this round): `hexList` single-use helper and an unreachable `?? 0` in `replay.ts`; redundant CAL ID length guard (kept for symmetry); spec contradiction on the `vin ` prefix resolved by orchestrator note.
- **Process rule (from deviation 6 and reviewer finding 4):** checks against the local discovery recordings must print counts only — never matched lines, line numbers with offsets, or decoded values. Both the implementer and the reviewer displayed derived data in their tool output (decoded values; and VIN serial positions recoverable from line-number matches), so the local session transcripts under `~/.claude/projects/-home-kema-projects-obd/` now contain data from which the full VIN can be rebuilt. No repo impact; owner may delete those transcripts.

### Stage C input (2026-09-24)

- The Mode 02 capture exists: `fixtures/recordings/chevrolet-equinox-ev-2024/2026-09-24-phone-console.redacted.jsonl`. Module 17 answered `020000` (L38) with `42 00 00 40 08 00 03` and `020200` (L42) with `42 02 00 00 00`. Other modules gave no reply. Stage C can source the reply layout from these lines.

### Stage C round 1 (Claude reviewer, 2026-09-24): APPROVE

- 7/7 Stage C items. `pnpm check` PASS (obd-core 141). The replay of all six tracked files matches HEAD except the two new L38/L42 lines. The bitmap and the recording bytes were checked by hand. Every constant is sourced: the recording lines, plus Wikipedia `oldid=1374011962` for `00 00` = no snapshot and PID 02 decoded as in service 3. Five of six mutations are caught. All four deviations and the `readingText` refactor are accepted, and Mode 01 output is byte-identical.
- 1. [minor, open, known gap] `freeze.ts:21`: the frame-number echo in `asMode01`, used by `decodeFreezePid` and `decodeFreezeSupported`, is untested. Only `decodeFreezeDtc` (:28) is exercised. Nothing is reachable today, because only frame 00 is ever requested. At the next touch, route both through one echo helper.
- 2. [minor, CLOSED by orchestrator] The Ownership block was stale; now updated.
- 3. [minor, optional] `ELM327.md:120` cites the tx lines 38 and 42; the reply bytes are on lines 39–40 and 43–44.
- Not run: a real stored-DTC freeze frame, a real Mode 02 PID value, and frames other than 00. These need a car with a stored DTC.

## Blocker

- None.

## Next action

- Owner commits Stages A and B. Stage C is unblocked (Mode 02 captured 2026-09-24).
- Minor cleanups (`hexList`, `?? 0`) at the next touch of `replay.ts`.
